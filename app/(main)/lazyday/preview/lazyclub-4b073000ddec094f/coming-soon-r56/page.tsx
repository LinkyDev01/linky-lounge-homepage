import { IntroR56 } from "./IntroR56"

/**
 * 라운드 56 시안 (임시 검토용) — 실 랜딩 /coming-soon 은 그대로 두고 여기서만 실험한다.
 * ① 빙고 써클 4스텝 와이프 ② 최종 그리드 광학 중심.
 * 운영자 확인 후 실 랜딩으로 이식하고 이 라우트는 정리한다.
 */
export default function LazyClubIntroR56Page() {
  return <IntroR56 />
}
