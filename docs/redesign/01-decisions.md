# 결정 로그 (최신 위)

- 2026-08-11 | 라운드 133 (운영자): **북클럽 인트로/포스터 대체 모션 — 포스터 기반 4안**
  (`/lazyday/preview/hero-motion`, 실사이트 미반영)
  · 지시: "인트로 또는 포스터 대체할 모션그래픽 … 지금 존재하는 포스터 바탕으로"
  · 재료 = 현행 4기 포스터(4th-poster-typo.webp)의 조형 그대로: 큰 글자 12자
    (레이지데이 북클럽 4기 모집) + 문장이 실처럼 얽힌 루프. 데모는 축소 재현 —
    채택 시 실제 포스터 좌표로 정밀 이식
  · ① 실선 인트로(createDrawable 1.4s + 글자 스태거, 총 ~2.2s 후 정지)
    ② 문장이 걸어 들어옴(textPath tspan 글자당 12ms — 실이 문장 그 자체)
    ③ 숨 쉬는 포스터(startOffset 60s/바퀴 존재 루프 — ①②의 도착 상태와 조합용)
    ④ 만지면 출렁이는 실(스프링 1회, 발견형). 데모 문장은 철학 원고 발췌 —
    실이식 시 포스터 원문으로 교체
  · 함정 기록: anime.js가 SVG `<text>`의 `y`를 **속성으로 트윈**한다 — 등장 모션은
    반드시 `translateY`로 (y 사용 시 글자가 y=0으로 이동해 잘림, 실측)
  · 확장 (운영자 "그거에 한정하지 않아도 좋아"): 포스터 무관 신규 3안 추가 → 총 7안
    ⑤ 자모 조립(초성 ㄹㅇㅈㄷㅇ ㅂㅋㄹ → 글자당 70ms 조립, ~1.4s)
    ⑥ 기수 카운트('N기 모집' 숫자 1→2→3→4 스탬프, ~0.9s — 기수 전환 시 숫자만)
    ⑦ 거북이 개장(거북이가 하단 횡단하며 주황 괘선을 남기고 퇴장, ~2.4s —
    캘린더·레이지클럽 거북이를 북클럽 인트로까지 잇는 시그니처)

- 2026-08-11 | 라운드 132 (운영자): **홈 추가 요소 기획 → 스크롤 거북이 프리뷰 +
  북클럽 아카이브 확장 4안**
  · 배경: 홈 추가 요소 기획안 10안 보고(그래픽 4/살아있는 블록 3/무용한 인터랙션 3).
    운영자 "기존 안의 것들에서 찾는 게 전부야?" → 아카이브 신규 발굴 병행,
    "거북이는 프리뷰 보여주고 그 사이트(animejs) 기반 레이지데이 북클럽도"
  · ① **스크롤 거북이 (레이지클럽 홈 프리뷰)**: 하단 고정 헤어라인 위 거북이가
    스크롤 진행률만큼 전진, 우측 결승선(캘린더 이중선 문법) 통과. 문구 없음(무표정).
    걸음(gait)은 스크롤 중에만 재생. `ScrollTurtle.tsx`+`scroll-turtle.module.css`
    (신규 모듈), 홈에만 마운트. 실측 390/1280: mid x=157/602, bottom=314/1204(레인 끝)
  · ② **북클럽 모션 — 아카이브 확장 4안** (`/lazyday/preview/motion-bookclub`,
    라운드 130 쇼케이스와 별개): ①스크롤 거북이 북클럽판(seek 매핑) ②페이지
    카운트업(오브젝트 트윈, 데모값 4,812) ③괘선 손긋기(svg.createDrawable, titleRow
    괘선 대체안) ④어절 스태거(철학 원고 확정 원문, 어절당 70ms). 전부 미반영 시안
  · 함정 기록: 스프라이트 scale 축소 시 **absolute bottom 앵커 필수** — 정적 배치면
    원본 높이(81px) 기준으로 그려져 발이 박스 밖(뷰포트 아래)으로 벗어난다

- 2026-08-10 | 라운드 131 (운영자): 제품 컬러 두 건
  ① **컬러 원 위치 확정 = 제품 이미지 바로 밑** — 실목록 반영. 카드 그리드에서 칩을
    이미지와 같은 열(1~2열)의 2행에 앉히고(`chipsUnderImg`) 정보 열은 1행 고정
    (`shopBodyRow`). 실측 1280·390: 칩 left = 이미지 left, top = 이미지 bottom
  ② **표현 방식 발산 6안** — `lazy-club.com/shop-colors` (내비 미등록, 셸 포함):
    A 도트(기준·현행) / B 종이 라벨(이미지 좌하단을 묾) / C 색띠 스트립(호버 구간 성장) /
    D 칩+이름 잉크(호버 시 색 이름) / E 셀렉터(선택 링+밑줄 물듦 — 커머스 대비) /
    F 물감 견본(비정형 스와치). 채택안만 실목록 이식 예정

- 2026-08-10 | 라운드 130 (운영자): **모션 확장 검토 — anime.js v4 아카이브 심층 검토 +
  시안 쇼케이스 4안** (`/lazyday/preview/motion-designs`, 실사이트 미반영)
  · 아카이브 검토: animejs.com 문서 402개 리프 페이지(각각 라이브 데모) 크롤 + 랜딩
    쇼케이스. 기능군 16종 확인 — 우리 문법과 맞는 것: createDrawable·createMotionPath·
    Draggable+spring·Timeline/stagger·Scope. 배제: 스크롤 스크럽(피로)·morphTo 남용(도형 금지)
    ※ 별도 CodePen 컬렉션(Poerqa)은 클라우드플레어 차단 — 같은 저자의 동일 데모군으로 갈음
  · 요구 명세 확정: M1 기존 요소만 움직인다 / M2 화면당 1모션·모션 없이 완결·1회성 /
    M3 무표정·발견형·불필요하게 고퀄, 같은 농담 반복 금지
  · 운영자 보정: 북클럽도 클릭 모션 원함(자주 오니 더 간결하게), 인트로는 **북클럽만**
    검토(상단 포스터 자리 대체 포함), 레이지클럽 인트로 신설은 안 함
  · 시안: ① 히어로 잉크 리빌(북클럽 인트로 대체, 1.1s 1회) ② 클릭 잉크 피드백(스프링
    눌림+잉크 점) ③ 거북이 물리(드래그→스프링 복귀, "거북이가 지름길을 거절했습니다.")
    ④ 하트 두근(호버 1회 두 박자). 의존성 animejs@4.5.0 추가(모듈 임포트, 프리뷰만 사용)
  · 검증: 4안 전부 인터랙션 실측 (FAQ 펼침 64px/+45°, 거북이 x210→드래그→x210 복귀+문구,
    하트 두근 중 scale 1.05 캡처)

- 2026-08-10 | 라운드 129 (운영자): 제품 목록·OG 세 건
  ① **이미지 불투명도 0.45 는 soldout 전용으로 복귀** — 코스터·머그(coming soon)는
    원래 불투명도. 브라운야드와 동일한 규칙이 됨
  ② **배지 세 제품 모두 NEW ARRIVAL** — "커밍 순 대신 뉴 어라이벌로 세 개 다".
    soldout(티셔츠)도 배지 유지, 품절은 정보 행 붉은 OUT OF STOCK 이 담당
  ③ **OG 제목 파이프 → 엠대시** — "레이지클럽 — lazy-club.com"

- 2026-08-10 | 라운드 128 (운영자 정정): 정보 행 순서는 **컬러 원이 맨 위** —
  컬러 원 → 상품명 → 가격 → 아웃오브스탁 (브라운야드 원본과 같은 배열).
  라운드 127 ③의 "상품명 먼저"는 오독이었다

- 2026-08-10 | 라운드 127 (운영자): 제품 목록 세 건
  ① **배지 서체를 브라운야드 문법으로** — Space Grotesk → 중립 그로테스크(Helvetica
     Neue 스택) 400 · 옅은 회색(#9b9184) · 12px. NEW ARRIVAL 표기 문법 그대로
  ② **아웃 오브 스탁 샘플 = 티셔츠** — status soldout. 좌상단 배지는 coming soon
     전용으로 좁히고, soldout 은 정보 행의 붉은 **OUT OF STOCK**(#a22525, 브라운야드
     실측색)이 맡는다 (중복 표기 방지)
  ③ **정보 행 순서 확정**: 상품명 → 컬러 원 나열 → 가격 → 아웃오브스탁 (각 행 분리)
  · 유의: 배지 회색이 코스터처럼 어두운 사진 위에선 흐리다 — 브라운야드는 흰 배경
    화보라 성립하는 문법. 제품 사진을 밝은 배경으로 통일하거나 배지색 조정 필요 시 후속

- 2026-08-10 | 라운드 126 (운영자): 제품 목록 두 건
  ① **가격 임의 표시 ₩999,999** — goods 3종 price = 999999 (확정가 나오면 교체).
     라운드 125 의 자동 전환 로직으로 "Coming Soon" 이 가격으로 바뀜
  ② **영문 배역 교체** — 상태 배지·가격 줄을 **Space Grotesk 500 · 전부 대문자 ·
     자간 0.07/0.05em** 으로 ("더 세련된 단정한 폰트로 모두 영어 대문자").
     셸 웹폰트 로드에 Space Grotesk 500 추가. ₩ 기호는 폴백(SUIT)이 그린다

- 2026-08-10 | 라운드 125 (운영자): **제품 목록 브라운야드 문법 개편** (레퍼런스
  brownyard.co.kr/product/list — 기존 셸·서체 틀 유지, 목록 카드만 이식)
  ① 상태 표기: 중앙 반투명 블랙박스(figOverlay) → **좌상단 소문자 라벨 + 이미지
     불투명도 0.45** (브라운야드 `.is-soldout{opacity:.45}` 실측 이식). 라벨 위치는
     coming soon/out of stock 공통 좌상단. 제품 목록만 — 모임 목록의 오버레이는 종전 유지
  ② **가격 줄 신설** — 이름 아래. 가격 확정 전엔 영문 "Coming Soon" (운영자 지정),
     가격이 들어오면 ₩n,nnn 자동 전환
  ③ **컬러 옵션 원형 칩** — goods-config.colors 신설, 운영자 제공 제품 사진에서
     실측 추출: 티셔츠 6색(#f0f0f2/#1a191f/#8488c0/#b17335/#d29e8f/#543021) ·
     머그 5색(#99bbab/#c9ad52/#c6a298/#8395a3/#5d5f5c) · 코스터 크림 1색(#f7ebc3).
     11px 원 + 잉크 헤어라인(밝은 색 대비)
  ④ **카테고리 태그("제품") 목록 미노출** — 품목이 적어 무의미(운영자 판단에 동의,
     별도 추천 없음). cat 필드는 상세 페이지·향후 세분류 대비 보존

- 2026-08-09 | 라운드 119~120 (운영자, 3건 일괄):
  ① **트랙 웹 버전 축소** ("전체 비율 맞춰 키우지 말고 트랙의 가로만 늘려" — 지난 구현은
     오해였다). viewBox 폭을 컨테이너/SCALE(0.665, 390 뷰포트 렌더 스케일)로 가변화,
     세로 렌더 스케일은 전 폭 고정 → **높이 130px 상수**, 가로만 359→720px.
     stadiumPath(dr, vbw) 파라미터화, 출발선 x = vbw/2, ResizeObserver 로 추적.
     글자도 모바일 값(10/11/12.5, line2 9.5px)이 전 폭 기본이 됨 — 데스크톱 확대분 폐기
  ② 문구 조사 교정: "거북이 보는 데" → **"거북이를 보는 데"**
  ③ **캘린더 브랜드 줄 풀네임** "레이지데이 북클럽" (운영자: 모바일은 잘려도 되지만
     PC 에선 다 보여야). 데스크톱 .chipBrand 는 어절 줄바꿈(normal+keep-all)으로 2줄
     "레이지데이 / 북클럽" 전부 노출, 모바일은 종전 nowrap 말줄임("레이지데이 북…") 유지
  ④ **시지프 신화 신청 차단** (실사이트 one-day-talk-01/apply): 세션에 `closed` 수동
     마감 플래그 신설 — 시각·마운트 무관 즉시 비활성, 배지 "마감"(지난 회차 "종료"와 구분).
     브람스는 이미 종료라 안내문 "현재 신청 가능한 회차가 없습니다" 노출

- 2026-08-08 | 라운드 118 (운영자 "데스크톱도 한 줄"): 1행 한 줄 조판을 **전 폭 기본 규칙**으로.
  라운드 117 의 모바일 전용 처리를 기본 `.line1` 로 올리고(nowrap + max-content +
  left:50%/translateX(-50%)), rowTop/rowMain 은 의미 단위(조건/답)로만 남기고
  display:inline. 미디어쿼리에는 행간·크기만 남는다.
  · 실측 1행 한 줄 폭 / 좌우 여유: 390px 254.4(±30.8) · 768px 458.9(±82.4) ·
    1024~1600px 458.9(±88.4 — 트랙이 720px 로 고정되어 그 이상은 동일)
  · 라운드 110 의 "못 박은 2행"은 이로써 종료 — 조건/답 구분은 색·무게가 계속 맡는다

- 2026-08-08 | 라운드 117 (운영자 "굳이 세어보면 – 남았습니다, 이것도 한 줄로"):
  **1행도 모바일에서 한 줄** (데스크톱은 라운드 110 의 못 박은 2행 유지 — 이번 지시는
  모바일 한 줄 논의의 연장으로 해석).
  · rowTop/rowMain 을 모바일에서 display:inline 으로 합치고(rowMain margin-left 0.3em
    이 블록 시절 줄바꿈 간격을 대신), line2 와 같은 nowrap+max-content+translateX 센터링
  · 크기를 vw 로 환산 (390px 기준 10/11/12.5px = 2.56/2.82/3.2vw, 하한 8/8.8/10px) —
    320px 급도 같이 줄어 안 넘친다
  · 실측 한 줄·좌우 여유: 1행 320px ±24.7 / 390px ±30.8, 2행 ±11.3 / ±18.2.
    문구 블록 34.8px — 구멍(75.7)의 절반 이하로 슬림해짐

- 2026-08-08 | 라운드 116 (운영자 "모바일에서 지금 폰트사이즈면 한 줄 가능해? 자간·장평
  살짝 건드려도 좋아"): **5분 문장 모바일도 한 줄로**
  · 라운드 113~114 의 두 줄 처리는 원인 오진 — 구멍이 아니라 문구 박스 폭(74%)이
    좁았던 것. 실측 390px: 한 줄 277.8 ≤ 그 높이의 구멍 폭 304.9
  · brMobile 줄바꿈 폐기. 모바일 .line2 = nowrap + width:max-content + 자간 -0.01em
    + font-size clamp(8px, 2.44vw, 9.5px) — 320px 급도 vw 로 같이 줄어 안 넘친다
  · 박스보다 넓어진 요소의 margin:auto 센터링은 overflow 시 좌측 정렬로 무너진다 →
    left:50% + translateX(-50%) 로 교정 (320px 우측 2.9px 넘침 해소)
  · 실측 (한 줄·좌우 여유): 320px ±5.1 / 360px ±11.4 / 390px ±13.5 / 600px ±101.5

- 2026-08-08 | 라운드 115 (운영자): 트랙 문구 서체·위계 재조정
  ① **크기 위계차 압축** ("구분은 하지만 강조 차이 정도는 줄여") — 라운드 110 의
     3단(21/29/40px, 1.9배)을 **18/20/23px(1.28배)** 로. 모바일 10/11/12.5px.
     구분은 색(주황/잉크)·무게(800)가 계속 맡고 크기는 반 발짝만
  ② **서체를 Gothic A1 600 으로** — 모임 설명 헤더(.itemTitle)·푸터(.footerDesc)와
     같은 배역. 강조 수치는 800. 웹폰트 로드에 800 추가(calendar/page.tsx),
     turtle 단독 시연 페이지에도 로드 추가
  · 실측 390px: 블록 67.6 ≤ 구멍 75.7 (아래 여유 4.1px), Gothic A1 적용 확인

- 2026-08-08 | 라운드 114 (운영자): 5분 문장 줄바꿈을 **"당신을 위해" 뒤로 못 박는다
  (모바일 전용)**. 자연 줄바꿈은 "완주를 / 서두르고"처럼 어중간한 곳에서 꺾였다.
  `<br className={brMobile}>` — 기본 display:none, max-width 600px 에서만 initial.
  데스크톱은 한 줄 유지(18.8px). 실측 390px: 줄 y 2개(788/801), 블록 69.5 그대로

- 2026-08-08 | 라운드 113 (운영자 확정): **트랙 2행 문구 개편 + 분 전환 10분 → 5분**
  · 30초~: `올해의 {N}초를 거북이 보는 데 쓰셨습니다.` — "올해의"가 1행이 세는 1년치
    초와 같은 단위계로 묶는다 (운영자 초안 "올해의 42초 동안 보고 계십니다"에서
    시제 충돌만 정리 — 완료형 "쓰셨습니다"로)
  · 5분~ (2행 교체): `{N}분째 보고 있는 당신을 위해 거북이가 완주를 서두르고 있습니다.`
    — 화면의 거북이는 명백히 기어가는 중인데 정색하고 서두른다는 무표정 거짓말.
    운영자 초안의 뒷문장("이 정도면 완주하실 것 같습니다")은 합의로 삭제 — 완주 중복·
    주어 모호·거짓말은 부연 없이 끝나야 셈
  · 전환점 600초 → **300초** (운영자 "너무 못 보면 아쉽잖아", 3분/5분 중 5분 채택):
    3분은 자연 도달이라 헌정의 과장이 덜 살고, 10분은 아무도 못 본다. 5분 = 의도적으로
    머문 사람만. 30초 문장이 세 자리 초("올해의 299초")까지 익을 시간도 남는다
  · 모바일 조판: 5분 문장이 두 줄 → 390px 블록 84.1 > 구멍 75.7 초과. max-width 600px
    에서 1행 반 단계 축소(lead 10 / year 13 / big 17.5px)·행간 1.25·2행 9.5px 로
    → 블록 69.5, 위아래 여유 3.1px. 볼드는 수치까지만(라운드 112 규칙) 유지

- 2026-08-08 | 라운드 112 (운영자 "xx초만 블랙 볼드하라는 거였어, 문장 전체는 아냐"):
  라운드 111 ②를 정정. 볼드·잉크는 **수치("42초"/"12분")까지만** — `.stayNum` 신설.
  문장은 다시 보조색 #a08b70 400 으로 되돌려 혼잣말 톤 유지. 조사(는/은)는 강조에서 뺀다
  (숫자+단위가 한 덩어리로 읽히게). 노출 30초는 그대로

- 2026-08-08 | 라운드 111 (운영자): 트랙 2행 두 건
  ① **노출 타이밍 10초 → 30초.** 10초는 1행을 읽는 도중에 끼어들고 "10초"라는 숫자가
     너무 작아 농담이 서지 않았다. 30초는 체류가 처음 의식되는 단위 — 1행을 다 읽은 뒤라
     "굳이 세어보면"이 회수된다. 트랙이 하단 고정이라 오래 머무는 방문자는 반드시 본다
  ② **2행을 잉크 블랙 700 으로** (운영자 "마지막에 추가문장은 블랙 볼드로").
     라운드 108 의 "한 단계 흐리게(#a08b70 400)"는 해제 — 크기 위계(한 단계 작게)는
     유지하고 무게로만 붙잡는다
  · 2행 유지 여부 판단(운영자 질문): **유지**. 1행만 두면 그냥 카운트다운이고, 2행이
    붙어야 "굳이 세어보면"이 회수되며 페이지가 자기를 관찰하는 농담이 된다. 구멍 안이라
    공간 비용 0px, 머문 사람에게만 보여 첫인상도 안 흐린다

- 2026-08-08 | 라운드 110 (운영자 "2027년까지 하고 줄바꿈 / 숫자들 서식은 문구 변경요청
  전처럼 연도와 초를 강조"):
  · 줄바꿈 위치는 라운드 109 에서 이미 `2027년까지` 뒤로 못 박아 둔 상태 — 유지 확인
  · **크기까지 라운드 89 실측으로 복귀.** 109 는 색·굵기만 되돌리고 상한을 15/20/28px 로
    잡아 데스크톱에서 예전보다 작았다 → 강조가 죽었다는 지적.
    라운드 89 는 SVG 단위 16/22/30 이 viewBox 스케일을 탔다 → 트랙 폭 대비 3.0 / 4.1 /
    5.6 vw, 트랙이 720px 로 고정되는 구간(뷰포트 ≥783px)의 실측치 **21 / 29 / 40px** 상한.
    모바일 하한 10.5 / 13.5 / 18.5px
  · 실측: 390px 문구블록 69.1 ≤ 구멍 75.7 / 1280px 114.9 ≤ 152. 세 행 모두 곡선 경계 안쪽

- 2026-08-08 | 라운드 109 (운영자): 두 건 — 캘린더 칩 조판 · 트랙 문구 재조판
  ① **칩 압축을 장평에서 글자 크기 쪽으로 옮긴다** (운영자 "장평 부자연스럽게 줄인 게
     애매해서 폰트사이즈를 좀 더 줄이고 장평·자간 재보정해").
     9.5px·장평 82%·자간 -0.01em → **8.5px·장평 96%·자간 -0.005em**.
     장평 96%는 눈에 띄지 않는다 — 82%가 글자를 홀쭉하게 만든 원인이었다.
     실측(390px, 가용 43px): 레이지데이 39.0 / 3기-n회차 34.6 / 시지프 신화·원데이 토크
     41.1 — 전부 말줄임 0. (여전히 잘리는 건 "브람스를 좋아하세요…" 78.7px 하나)
  ② **트랙 문구를 못 박은 2행으로** (운영자 "한 문장이 두 줄이라는 게 감 떨어지고,
     초에 집중이 안 되네 / 굳이 세어보면 – 남았습니다. 여기까지 하고 나머지 년과 초
     서식은 이전 버전처럼").
     · 라운드 108의 한 문장 흐름 폐기. 1행 `굳이 세어보면 2027년까지`(조건) /
       2행 `12,530,761초 남았습니다.`(답). 각 행 `white-space: nowrap` — 자연 줄바꿈 없음
     · 문구도 운영자 수정본으로: "세어보니"→**"세어보면"**, "남아 있습니다"→**"남았습니다"**
     · **라운드 89 서식 복귀** — 2027년 주황 800 (clamp 13→20px), 초 숫자 800
       (clamp 18→28px), 나머지 문장은 보조색 #8a7660. 라운드 108 의 "강조색 없음"은
       이 지시로 해제 (충돌 시 최신 지시 우선)
     · 2행(체류)은 라운드 108 그대로. 구멍(390px 기준 324.6×77.4) 안에 3행 63.9px —
       세로·가로 모두 이탈 없음 실측

- 2026-08-08 | 라운드 108 (운영자): **거북이 트랙 카운트다운 문구 2행** (무심한 유머)
  · 자리 = 스타디움 **안쪽 구멍**("정확히는 트랙 안에"). 원래 비어 있던 공간이라
    섹션 높이 증가 **0px** — 지오메트리(viewBox 540×196) 무변경. 운영자 질문
    "하단 공간차지가 지나치게 늘어져?"에 대한 답: 안 늘어난다 → **두 문장 다 유지**
  · 문구는 운영자 지정 원문 고정:
    1행 `굳이 세어보니 2027년까지 {남은초}초가 남아 있습니다.`
    2행 `이 중 {체류초}초는 이 페이지에서 쓰셨습니다.` (>600초면 `{n}분은`)
  · 2행 = **보이는 동안만** 쌓인 체류 시간. visibilitychange 로 숨은 구간을 통째로
    버린다 (백그라운드 탭에서 쌓이면 문장이 거짓이 된다). 새로고침 시 0부터.
    10초에 300ms 페이드로 등장, 1행보다 한 단계 작게(12.5/15px)·흐리게(#a08b70)
  · **SVG `<text>` → HTML 로 전환**. viewBox 스케일을 타면 모바일에서 10px 남짓이 되어
    못 읽는다. `.trackBox` position:relative + `.readout` 절대배치(top 54.6% = 구멍 중심
    y107/196), clamp 로 크기 보장. 실측 390px: 구멍 324.6×77.4 안에 텍스트 271×53.6
  · ⚠️ **라운드 89의 '2027년' 주황 강조 해제** — 이번 사양의 "강조 색상 넣지 말 것,
    무표정한 톤이 핵심"이 상위. 숫자도 색·굵기 강조 없이 tabular-nums 만 (폭 흔들림 방지)
  · 모바일에서 1행은 두 줄로 흐른다(`keep-all` 어절 단위). 한 줄에 넣으려면 9px 대가 되어
    포기 — 구멍 안에 3줄이 다 들어가고 잘리지 않는 것을 실측 확인
  · 검증용 쿼리 `?stay=<초>` 추가 (9/10/600/725 경계 확인). 숨김 검증: 12초 노출 → 11,
    6초 숨김 → 11 유지, 4초 복귀 → 15

- 2026-08-08 | 라운드 107 (운영자):
  ① **무비토크도 가격 미노출** — `『호프』` program price = "". 참가비 문의 창구는
     CTA("문의하기 ↗", 카카오 채널)가 맡는다. 이로써 캘린더 목록에서 가격을 노출하는 건
     원데이 토크(35,000원) 뿐
  ② **목록 카드 제목에서 종류어 제거** — 카테고리 줄이 이미 "원데이 토크"·"무비토크"라
     제목의 "… 원데이 토크"는 중복이었다 → `『시지프 신화』`·`『브람스를 좋아하세요...』`·
     `『호프』` 로 작품명만
  ③ **괄호(『』) 대체문자 판단: 대체하지 않는다 (권고안)** — 운영자 "폭이 길면 대체문자 사용 가능".
     현재 방식은 이미 폭 문제를 **위치로** 나눠 풀고 있다: 좁은 캘린더 칸은 `workTitle()`로
     괄호를 빼고, 폭이 넉넉한 목록 카드에서만 괄호를 쓴다. 목록에서 「」·<>·따옴표로
     바꿔도 절약되는 폭은 카드 폭의 2% 남짓이라 실익이 없고, 사이트 전반이 쓰는
     『』 관례만 깨진다 → 현행 유지
  ④ 모바일 칩 **장평 0.88 → 0.82, 둘째 줄(`.chipRound`)에도 동일 적용** — 라운드 106의
     2줄 서식이 원데이/무비 칩에서 첫 줄 "시지프 신화"(6자)·둘째 줄 "원데이 토크"(6자)
     둘 다 말줄임 났다. 0.82 로 6자까지 들어간다 (390px 실측 확인).
     여전히 잘리는 건 "브람스를 좋아하세요..."(11자) 뿐 — 물리적 한계로 수용

- 2026-08-07 | 라운드 106 (운영자):
  ① **레이지데이 북클럽은 캘린더에서 가격 미노출** — 4기·3기 program price = "".
     (원데이 토크·무비토크는 종전대로 표기)
  ② **원데이 토크·무비토크도 기수와 같은 2줄 서식** — `cellLines = [작품명, 종류]`.
     첫 줄 작품명 / 둘째 줄 "원데이 토크"·"무비토크"(보조색, 기수의 "3기-2회차" 자리와 동일).
     · 작품명은 `『』` 를 뺀다 (`workTitle()` 로 괄호 안만 추출) — 51px 칸에서 괄호 두 칸이
       아까워 한 글자라도 제목에 쓰는 편이 식별에 낫다
     · 무비토크는 지시에 없었지만 같은 문법으로 통일 (혼자 다른 서식이면 어색)
  · 남은 말줄임: 긴 책 제목 2건("브람스를 좋아하세요…", "시지프 신화")은 43px 칸에
    물리적으로 안 들어간다 → 자연 말줄임. 장평(88%)은 반복 문자열인 "레이지데이" 기준으로
    맞춰 두고 가변 길이 제목은 잘리게 둔다 (종류 줄과 색이 의미를 보완)

- 2026-08-07 | 라운드 105 (운영자):
  ① **3기 시간대 확정** (운영자 제공) — 수·목 19:30–22:30 / 일 오전 10:30–13:30 ·
     오후 14:30–17:30. `SEASON3_TIMES` 로 3기 카드에 표기 (4기와 같은 문법)
  ② **자간 대신 장평** — 라운드 104는 브랜드 줄을 자간 -0.045em·9px 까지 조여 답답했다.
     운영자 "무리한 자간 좁히기보다는 장평도 건드려" → **장평 88%** 로 전환하고
     글자 크기는 회차 줄과 같은 9.5px 로 복귀, 자간은 -0.01em(사실상 원래대로).
     ⚠️ 핵심 기법: `transform: scaleX()` 만 쓰면 **레이아웃 폭이 그대로**라 말줄임이 계속 뜬다.
     `width: calc(100% / var(--sx))` 로 레이아웃 폭을 먼저 1/S 만큼 넓혀 overflow 를 없앤 뒤
     `scaleX(S)` 로 되돌려 그린다. 실측: scroll 49 = client 49(말줄임 0), 그려진 폭 43.2px

- 2026-08-07 | 라운드 104 (운영자): 캘린더 칸 표기 3건
  ① 『호프』 무비토크 **8/2 → 7/26**(일주일 전 일요일). 7월로 넘어가 8월 목록에서 빠진다
  ② **모든 칩이 두 줄까지** — 라운드 94의 `:only-child` 한정 폐기. 데스크톱도 두 줄,
     행 높이는 내용따라 자란다 ("반응형 행추가"). 데스크톱 셀은 아직 92px 안에 들어옴
  ③ 정규 기수 칸 표기를 **["레이지데이", "3기-1회차"] 고정 2줄**로 (운영자 지정).
     `ClubEvent.cellLines` 신설 — 자유 줄바꿈(line-clamp)이 아니라 줄을 못 박아
     브랜드/기수가 뒤섞이지 않는다. 회차 줄은 보조색
  · ⚠️ "레이지데이"(5자)가 모바일 51px 칸에서 잘렸다(48 > 38px). 운영자 지시대로 압축:
    **페이지 좌우 여백 20→12 · 셀 안쪽 2→1 · 칩 padding 회수**로 가용폭 38→43px 확보한 뒤
    브랜드 줄만 `font-size 9px + letter-spacing -0.045em`. 실측 43/43 — 잘림 0건.
    (scaleX 는 레이아웃 폭이 그대로라 ellipsis 가 계속 떠서 부적합)

- 2026-08-07 | 라운드 103 (운영자, 레퍼런스 p-i-e.kr/schedule): **거북이 트랙 하단 고정** —
  신청하기 스티키 CTA 문법처럼 상시 노출. `position: sticky; bottom: 0` 하나로:
  스크롤 내내 뷰포트 하단 밀착 → 끝까지 내리면 제 문서 위치에 자연 도킹("정상 궤도").
  fixed 가 아니라 sticky 라 도킹 분기 코드가 없다. 종이색 배경 + 상단 헤어라인으로
  밑을 지나는 콘텐츠와 분리, z-50. 폭 = 캘린더 폭 규칙 유지.
  실측: 상단·중간 스크롤에서 slot bottom = 뷰포트 높이(844/900), 맨 아래에선 자연 위치(764/820)

- 2026-08-07 | 라운드 102 (운영자): 포스터 없는 카드도 **이미지 빈자리 유지** —
  라운드 100 ③('칸 접기')을 **철회**. 카드끼리 본문 시작선이 어긋나던 게 더 거슬린다는 판단.
  회색 면은 빼고 **옅은 점선 자리**로만 표시해 미완성처럼 보이지 않게 한다 (`.thumbEmpty`).
  라운드 100에서 특이도 때문에 넣었던 `.item.itemNoThumb` 는 함께 제거.
  실측: 카드 4장 본문 시작 x 좌표 전부 일치 (데스크톱 176 / 모바일 91)

- 2026-08-07 | 라운드 101 (운영자): 캘린더 칸 **최대 3건 + 모바일 2줄**
  · "하루에 세 모임까지는 표시되어야" → `MAX_CHIPS` 2 → **3**. 실데이터 최대치가
    하루 3건(8/2: 3기 2회차 + 브람스 + 무비토크)이라 `+N` 표기가 사라진다
  · "모바일에서 두 줄에 걸쳐서?" → **추천대로 반영**. 51px 셀에서 한 줄은 4글자뿐,
    두 줄이면 8글자 안팎이 읽힌다. `-webkit-line-clamp: 2` 라 짧은 제목은 한 줄로
    끝나 셀이 필요 이상 자라지 않는다 (실측: 대부분 행 64px, 3건인 행만 109px)
  · ⚠️ 줄바꿈 규칙: `word-break: normal` 은 한글을 글자 단위로 끊어 "3기 2회 / 차 레…"
    처럼 어절 중간이 깨진다. **`word-break: keep-all` + `overflow-wrap: anywhere`**
    조합이라야 어절 우선으로 끊고("3기 / 2회차…"), 칸을 넘는 긴 어절만 글자 단위로 쪼갠다

- 2026-08-07 | 라운드 100 (운영자): 캘린더 시안 **톤 정합 5건** (자가 진단 → 전건 반영)
  ① 카드 제목 SUIT 700 → **Gothic A1 600** (홈 `.itemTitle` 과 같은 배역).
     단독 시안이라 셸이 없어 웹폰트 `<link>` 를 page.tsx 에서 직접 로드, `--gothic` 도 자체 정의
  ② 상태 표기: 테두리 배지 폐기 → **색 텍스트**만 (모집 중 주황 / 진행 중 본문색 / 마감 회색).
     이 트리는 상태를 색으로 말한다
  ③ 포스터 없는 카드(무비토크)의 **빈 회색 칸 제거** — 본문이 전폭.
     ⚠️ `.itemNoThumb` 만으로는 뒤에 오는 미디어쿼리 `.item{grid-template-columns:60px 1fr}`
     에 밀려(같은 특이도 → 후순위 승) 모바일에서 본문이 60px 칸에 갇혀 한 글자씩 세로로
     쪼개졌다 → **`.item.itemNoThumb`** 로 특이도 상향
  ④ 모바일 칩: 색면 채움(구글 문법) → **좌측 색 괘선 + 투명 배경 + 잉크 글자**.
     괘선 문법을 지키면서 가독성 유지
  ⑤ 선택한 날: 주황 면 채움 → **옅은 주황(α .11) + 날짜만 주황 800**.
     면 채움이 조판 톤을 눌렀고, 옅게 가니 칩 글자 반전 처리도 불필요해졌다

- 2026-08-07 | 라운드 99 (운영자): **모임 ↔ 개별 일정 분리** + 3기 반영
  · "한 모임은 한 포스터와 정보만. 다른 날이라고 정보까지 계속 놓는 게 아냐" →
    자료구조를 둘로 쪼갠다. **ClubProgram**(모임 한 건 = 포스터+정보 한 벌, 아래 목록이
    렌더) / **ClubEvent**(개별 일정 한 날, 캘린더 칸 전용). 정규 기수는 회차·요일이
    몇 개든 카드 하나. 라운드 97의 슬롯 분할(일 오전/오후를 별도 이벤트로)은 폐기 —
    슬롯 정보는 기수 카드의 `times` 로 이관
  · 캘린더 칸 표기 = **"n기 n회차 레이지데이 북클럽"** (운영자 지정 형식)
  · **3기 추가** — ⚠️ season-config 는 현재 기수만 담아 지난 기수 회차 날짜는 단일 출처가
    없다. 운영자 제공 구글 캘린더의 '레이지데이' 일정 + book-config 의 3기
    dateRange("7.15 – 9.6")로 확정: 수·목·일 격주
    7/15·16·19 / 7/29·30, 8/2 / 8/12·13·16 / 8/26·27·30 / 9/6.
    스크린샷과 전수 일치 확인. **3기 시간대·참가비는 미제공이라 비워 둠**
  · 기수 카드에 상태 배지(모집 중 / 진행 중 / 마감)와 요일별 시간대 줄 추가
  · 검증: 8월 = 칸 8개(3기 개별 일정) + 카드 4장(3기 하나로 묶임 · 원데이 2 · 무비 1),
    9월 = 칸 7개 + 카드 2장(4기·3기)

- 2026-08-07 | 라운드 98 (운영자): 무비토크 정보 확정 — **8/2 19:00, 참가비 "문의"**.
  (종료 시각 미제공이라 시작만 표기)
  · 참가비가 금액이 아니라 문의라 **문의 창구가 필요** → 사이트 기존 카카오 채널
    (`SEASON.notifyKakaoUrl`)로 연결. 새 창구를 만들지 않고 있는 것을 재사용
  · `ClubEvent.cta` 신설 — 기본 "자세히 보기", 무비토크만 "문의하기".
    (전용 상세 페이지가 생기면 href·cta 교체)

- 2026-08-07 | 라운드 97 (운영자): 북클럽 4기 **화·수·일 슬롯을 각각 독립 일정으로**.
  `season-config` 의 일요일 time 은 `"10:30–13:30, 14:30–17:30"` 처럼 **2슬롯이 한 문자열**
  (config 주석: "일요일은 오전·오후 2슬롯 — 표에서는 줄로 나눠 렌더"). 라운드 95는 이걸
  한 줄로 합쳐 보여줘 오전/오후 구분이 사라졌다 → `","` 로 쪼개 각각을 이벤트로 세운다.
  · `ClubEvent.slot` 신설 — "화요일" / "수요일" / "일요일 오전" / "일요일 오후" (목록 메타에 표기)
  · `ClubEvent.cellLabel` 신설 — 같은 날 두 슬롯이 겹쳐도 구분되게 날짜 칸은
    "1회차 일 오전" / "1회차 일 오후" 처럼 회차+요일(+오전/오후)로 표기.
    원데이 토크·무비토크는 제목 그대로
  · 같은 날은 시간순 정렬. 결과 9월 6건 → **8건** (일요일 2회×2슬롯 반영)

- 2026-08-07 | 라운드 96 (운영자): 캘린더 마감 2건
  ① **거북이 트랙 → 맨 아래 섹션** — 캘린더·범례·모임 목록 다음, 페이지 마지막.
     폭 = 캘린더 폭 규칙은 유지
  ② **모바일에도 셀 텍스트** — "구글은 그래도 보이던데" → 점 모드 폐기,
     구글 캘린더 문법의 **알약 라벨**(카테고리 색면 + 종이색 글자 9.5px + 말줄임,
     셀 전폭)로. 50px 셀에서도 3~5글자("레이지…")는 읽히고 전문은 아래 목록이 담당.
     구현: 칩 색을 CSS 변수(--cat)로 넘겨 데스크톱=점+제목, 모바일=알약으로
     같은 마크업이 미디어쿼리에서 변신. 단독 모임 두 줄 확장은 알약에선 해제
     (셀 높이 균일 우선). 선택(주황 면) 위 알약은 종이색 테두리로 분리

- 2026-08-07 | 라운드 95 (운영자): 캘린더 시안에 **실일정 투입** — 가짜 표본 폐기
  (`calendar-preview-data.ts` 삭제 → `calendar-events.ts` 신설)
  ① **레이지데이 북클럽 4기** — `season-config.ts` 의 `sessions`/`fifth` 에서 파생.
     하드코딩 금지: dates 배열 순서 = `SEASON.days` 순서(화·수·일)라는 config 계약을
     그대로 써서 날짜별 시간대를 매핑 (화·수 19:30–22:30 / 일 10:30–13:30, 14:30–17:30).
     각 회차가 세 요일 중 택1 구조라 세 날짜 모두 캘린더에 올린다.
     검증: 9월 9·13·15(1회차) 23·27·29(2회차) / 10월 7·11·13(3회차) 21·25·27(4회차) /
     11월 1(5회차 19:00–22:00) — 운영자 제공 4기 일정표와 일치
  ② **원데이 토크 2건** — `one-day-config.ts` 에서 파생 (브람스 8/2, 시지프 8/9).
     운영자 표현 "원데이 클럽"은 config 확정 명칭 "원데이 토크"로 표기
  ③ **무비토크** — "8/2 호프 - 무비토크". 전용 config 가 없어 `MOVIE_TALKS` 배열에 임시
     보관(생기면 이관). 시간·참가비 미제공이라 **비워 둔다** (임의 생성 금지) →
     UI가 빈 값을 감추도록 처리
  · 카테고리 3종을 승인 팔레트 안에서 배정: 북클럽 #d2691e / 원데이 토크 #845d5e /
    무비토크 #96ab9b. 범례는 그 달에 실제로 있는 종류만 노출
  · 구글 캘린더 훅(`useGoogleCalendarMeetups`) 경로는 이 시안에서 미사용 —
    config 들이 이미 확정 일정을 보유. **실이식 때 소스 선택(구글 캘린더 vs config)은
    운영자 결정 사항**으로 남긴다
  · 기수 전환 시 season-config 만 고치면 캘린더가 따라온다

- 2026-08-07 | 라운드 94 (운영자): 캘린더 셀에 **텍스트 정보 노출** — 점만으로는
  무슨 모임인지 알 수 없다는 지적.
  · 폭 확보: `.wrap` 560 → **720** (셀 80 → 약 103px). 트랙은 width:100% 라 자동 추종
  · 셀 안에 `제목 줄`(점 + 제목) 최대 2건 + 초과분 `+N`. 그 날 모임이 **하나뿐이면
    두 줄까지** 풀어 쓰고(`:only-child`), 둘 이상이면 각 한 줄 — 셀 높이 균일 유지
  · 좁은 화면(≤600px)은 셀 폭 50px 라 텍스트 불가 → **점 모드로 환원**.
    달의 개요는 점이, 내용은 아래 목록이 텍스트로 담당하는 역할 분담
  · ⚠️ 함정: `grid-template-columns: repeat(7, 1fr)` 의 1fr 최소값은 min-content 라,
    셀 안 `nowrap` 제목이 열을 밀어 격자가 컨테이너를 넘쳤다(셀 실측 206px).
    **`minmax(0, 1fr)`** 로 고정해야 한다
  · 실측: 데스크톱/태블릿 셀 103px·텍스트 모드, 390px 셀 50px·점 모드,
    트랙 폭 일치 720/720 · 350/350

- 2026-08-07 | 라운드 93 (운영자): **MeetupCalendarSection 레이지클럽 톤 시안** —
  `preview/lazyclub-…/calendar` 신설. 원본(`components/lounge/MeetupCalendarSection.tsx`,
  linkylounge 홈에서 주석 처리된 채 잠자던 740줄)은 **건드리지 않고** 별도 구현.
  · 데이터 계약 계승: `useGoogleCalendarMeetups(year, month)` 그대로, 매핑 키는 `meetup.day`
  · 톤 이식: Tailwind sage/rose·민트 CTA·rounded-2xl+그림자+리프트 호버 →
    **오트/잉크/주황 + 1px 괘선 조판**, SUIT 단일, 장식 0. CSS Module 자체 보유
    (기존 .module.css import 금지 규율 준수)
  · 카테고리 색은 이 트리 안에서만 재정의(`CATEGORY_TONE`) — 공유 상수
    `constants/lounge/categories.ts` 는 linkylounge 소비자가 있어 미변경
  · 덜어낸 것: 모바일 3D 캐러셀(blur·rotateY) → 세로 목록 하나로 통일
  · 고친 것: 날짜 셀을 `<div onClick>` → **`<button>`**(원본은 키보드 접근 불가),
    월 이동 aria-label, `role=grid`/`aria-pressed`/`aria-current`, 로딩 `role=status`
  · ⚠️ 분석 이벤트 4종은 시안에서 미호출 — 실이식 시 원본과 같은 이름·파라미터로 복원할 것
  · 폴백 상수 `MEETUPS` 가 2026-01 자료뿐이라 늘 빈 달로 보임 → 시안 전용 표본
    (`calendar-preview-data.ts`)을 보고 있는 달에 깔았다. 훅이 실데이터를 주면 자동으로 밀려남
  · **트랙 폭 = 캘린더 폭** (운영자): `.stage` 를 `width:100%` 로 바꿔 부모를 따르게 하고
    캘린더와 같은 `.wrap` 안에 배치. 실측 데스크톱 560/560 · 모바일 350/350 일치.
    단독 시연 페이지는 `.demoWrap` 이 폭을 잡는다

- 2026-08-07 | 라운드 92 (운영자): 전체보기 홈 **맨 아래 아카이브 캐러셀 일단 숨김** —
  `SHOW_ARCHIVE = false` 로 렌더만 차단 (SHOW_GOODS 와 같은 문법). book-config 데이터·
  캐러셀 구현은 그대로, 숨김 중엔 자동 넘김 타이머도 정지. 내비 '아카이브'(→ /archive
  별도 페이지)는 영향 없음 — 홈 섹션은 모임 · 굿즈 · 레이지데이 북클럽 3개로

- 2026-08-07 | 라운드 90~91 (운영자): 거북이 트랙 마감
  ① 캡션 "2026년이 지나간 만큼…" **삭제** (트랙만 남김, `.caption` CSS도 제거)
  ② 걸음 **더 느리게** — 400ms/프레임 유지 기조
  ③ **발 바뀌는 지점 보정** (프레임 보간은 운영자가 거부 — "느릿느릿 뻣뻣한 모션이 더 좋아")
     · 원인 규명: 레퍼런스 GIF 8프레임은 실은 **고유 포즈 5개의 왕복** —
       5·6·7이 3·2·1과 픽셀 단위 완전 동일(diff 0.000). 포즈 거리 행렬상
       P0…P4는 닫힌 루프가 아니라 **선형 스윙**(d(P4,P0)=26.5 로 최대) → 왕복이 맞는 구조.
       문제는 왕복을 프레임으로 구워 둔 탓에 양 끝(P0·P4)이 **딱 한 박자만** 찍히고
       즉시 되돌아와 발이 튕기듯 보인 것
     · 해법: 중복 3장 폐기 → 고유 5장 + CSS `alternate`. 양 끝이 자동으로 두 박자
       유지되어 "딛고 무게 옮긴 뒤 방향 전환" 리듬이 된다.
       실측 재생 순서: 3(400ms) → **4(800ms)** → 3 → 2 → 1 → **0(800ms)** → 1 → 2 …
       편도 2s·왕복 4s, 프레임당 400ms 유지. 스프라이트 1120×81 → **700×81**
  · ⚠️ 함정: 스프라이트 스트립을 받으면 **프레임 중복 여부부터 검사**할 것.
    왕복이 구워져 있으면 CSS alternate 로 되돌려야 끝점 리듬이 산다

- 2026-08-07 | 라운드 89 (운영자): 거북이 트랙 마감 3건 — **용도 확정: 캘린더 아래 배치**
  ① **'2027년' 강조** — tspan 분리: 2027년 = 주황 #d2691e·800·22px, '까지'는 보조색 16px
  ② **앞다리 끝선 = 채움 경계** — 가로 앵커를 몸 중심(-70)에서 앞발 끝(셀 x≈106,
     8프레임 실측 평균)으로. 머리(x≈136)만 회색 쪽으로 살짝 나간다 (운영자 허용)
  ③ **세로 슬림화** — 곡선 반경 110→70·직선 좌우 연장(88↔452)·viewBox 내용 크롭
     (720×400 → 540×196) + stage gap 28→18. 모바일 366px 뷰포트에서 트랙 높이 ≈133px

- 2026-08-07 | 라운드 88 (운영자): 거북이 트랙 다듬기 3건
  ① **거북이 속 채움** — 몸이 트랙을 비추던 투명 문제. 플러드필로 외부/내부를
     구분해 외부만 투명, 실루엣 내부는 **오트(#f7f3ee) 불투명**(선은 잉크, 경계는
     혼합). 스프라이트 재생성 (65KB)
  ② **트랙 슬림화** — 밴드 44 → 26, 차선은 **가운데 파선 하나만** (3레인 폐기).
     거북이가 트랙 위에 올라서는 디자인 관점 수용
  ③ **뒤집힘 금지** — 왼쪽으로 갈 때 회전 180° 대신 **좌우 미러 + 기울기 ±90°
     클램프**: 상단 직선에선 미러로 진행 방향을 보며 꼿꼿이, 곡선에선 기울어져
     오르내림. 발 앵커(-76)를 경로선에 얹어 항상 트랙 위에 선다

- 2026-08-07 | 라운드 87 (운영자): **거북이 프로그레스 = 트랙형 재설계** (직선 바 폐기)
  ① 의미 부여: **2027년 1월 1일(KST)까지 실시간 카운트다운** — 중앙에 "2027년까지
     N초"(초로만, tabular-nums, 1초 갱신). 거북이 위치 = 2026년 경과 비율 —
     1년짜리 트랙이라 아주 천천히 꾸역꾸역 (다리는 계속 젓는다)
  ② 밴드 두께 3px → **44유닛** (레인 3개 + 종이색 파선 차선 + 안팎 테두리)
  ③ 거북이·채움 동기: 둘 다 같은 frac 에서 유도 (getPointAtLength ↔ dashoffset) —
     앞서거나 늦을 수 없는 구조
  ④ **스타디움 루프** (레퍼런스: 육상 트랙 톱뷰) — 결승선 = 출발선(하단 중앙,
     종이색+체크 파선 이중선), 회색 밴드 위에 주황 루트가 경로를 따라 차오름.
     반시계(육상 방향). SVG viewBox 반응형 — 모바일에서 거북이 ~34px 로 동반 축소.
     거북이는 **트랙 속**: 접선 회전 + 발이 항상 바깥쪽 (상단 직선에선 뒤집혀
     보이는 것이 루프의 정직한 문법 — 보드게임 말 방식, 대안 검토 여지 명시)
  · 검증: frac 0.05/0.25/0.5/0.75/0.98 + 실시간(8월≈60%) + 390px 캡처.
    ?frac= 위치 고정, ?speed= 가속 파라미터 제공

- 2026-08-07 | 라운드 86 (운영자):
  ① **거북이 앞다리 꼬임 보정 = 8프레임 교체** — 구 5프레임 스트립은 프레임 0의
     앞다리가 X자로 교차(몸통 내부가 투명이라 뒷선이 관통해 보임). 운영자 레퍼런스
     GIF의 8프레임이 꼬임 없는 깨끗한 사이클이라 **그대로 스프라이트화**
     (셀 140×81, 캔버스 등록 공유 → 공통 크롭만 적용해 걸음 튐 없음).
     gait 2.4s steps(8) = GIF 실측 300ms/프레임
  ② **맥동 그림자 핸드오프** — 인트로 종료 순간 그림자가 '툭' 나타나던 이음새:
     "처음엔 없다가 진해지기 시작할 때부터 자연스럽게 생기고 이후 반복" 지시대로
     밝기(alternate-reverse 유지)와 그림자를 분리 — …ShadowIn(2s 단발, 후반 1초 상승)
     → …ShadowPulse(2s 지연 무한 alternate). 같은 속성은 목록 뒤 Pulse 가 2s부터
     제어권 인수, 경계값 일치(글자 .3/써클 .5)로 이음새 없음. 타임라인 실측:
     0~1s 그림자 0 → 1~2s 0→최대 상승 → 이후 옅게↔진하게 반복 확인

- 2026-08-07 | 라운드 85 후기: 병합 커밋(61b0396)의 Vercel 프로덕션 배포가 생성되지
  않아(웹훅 누락 추정) 본 커밋으로 재트리거. §5 함정 추가 후보 — 병합 후 프로덕션
  폴링이 오래 조용하면 list_deployments에서 main 배포 존재부터 확인할 것

- 2026-08-07 | 라운드 85 (운영자):
  ① **내비 = 페이지 분리 원칙** — "전체보기 페이지 하나에서 섹션이동이 아니라"
    각 항목은 **각각만 목록화된 전용 페이지**: 전체보기 → 홈(전체) / 모임 →
    `/meetings`(원데이토크만) / **제품 → `/shop` 신설**(굿즈 3종만, 모임 목록과
    같은 리스트 문법 — ShopIndex.tsx) / 아카이브 → `/archive`.
    제품의 홈 앵커(#shop) 링크 폐기
  ② **거북이 걸음 속도** — 운영자 레퍼런스 GIF 실측 프레임당 300ms →
    다리 사이클 0.7s → **1.5s**(5프레임×300ms). 선 제거·투명 배경은 기존 가공이
    이미 충족 (배경 알파화 + 바닥선 없는 프레임)

- 2026-08-07 | 라운드 84 (운영자): **거북이 프로그레스바 시연** — 임시 라우트
  `lazyclub-…/turtle`. 운영자 제공 5프레임 워크 사이클 스트립을 가공해
  거북이가 프로그레스바 위를 걸어 전진 (14s 완주 무한 반복, 다리 steps(5) 0.7s,
  주황 채움과 동기). 채택 시 실제 프로그레스(스크롤·폼 단계 등)에 연결해 이식.
  · 스프라이트 가공: 등분 절단은 이웃 머리·꼬리 조각이 섞임(간격 불균등) →
    **어두운 픽셀 열 기준 세그먼트 탐지**로 5마리 분리, 오프화이트 배경 알파화(>230 투명),
    발끝 하단 정렬 + 셀 여백 12px → `public/...home-v3/turtle-sprite.png` (700×78, 37KB)
  · 내비 '전체보기' 재확인 (라운드 83 반영분 유지)

- 2026-08-07 | 라운드 83 (운영자):
  ① **모바일 푸터 이용약관 = SNS 바로가기 바로 윗줄** — 우상단 코너(라운드 74 배치)를
     모바일에선 버리고, 연락처 블록 안 `.contractsInline`(인스타·카카오 아이콘 바로 위,
     좌정렬 일치)로. 데스크톱은 종전대로 우측 끝 `.contracts` 유지 — 표기 2벌을
     media query로 상호 배타 노출 (한쪽만 보임, DOM 상시 2개)
  ② **내비 맨 왼쪽 '전체보기' 신설** — 전체보기 → 홈 전체(BASE), 모임은
     모임 목록(`/meetings`)으로 재지정. 좌측 4항목(전체보기·모임·제품·아카이브)
     모바일 390px에서도 우측과 충돌 없음 측정 확인 (좌끝 242 < 우시작 276)
  · 거북이 프로그레스바: 운영자가 가로 5프레임 원본 업로드 (`turtle-strip.png` 로 보관)
    — 프로그레스바 위치 지정 대기 중

- 2026-08-07 | 라운드 82 (운영자):
  ① **제품 부활** — 내비 모임과 아카이브 사이 '제품'(→ 홈 굿즈 섹션 `#shop` 앵커) +
     굿즈 블록 `SHOW_GOODS=true`. 라운드 78의 보류를 플래그 한 줄로 해제 (보존 원칙 입증).
     "지운 게 아니라 남겨야 해" 재확인 — 앞으로도 섹션 제거는 렌더 차단으로만
  ② **모임 태그 한국어화** — `booktalk` → **원데이토크**. `CATEGORY_LABELS`
     (one-day-config 단일 출처) 신설, 홈·모임 목록·모임 상세 3곳 일괄 적용
     (분류 키는 영문 유지 — 표기만 변경)
  ③ **북클럽 기수 카드 태그 = 기수별 일정** (운영자 제공 문자열 그대로):
     4기 9/7-11/1 · 3기 7/15-9/6 · 2기 5/21-7/12 · 1기 3/19-5/17.
     ※ 4기 일정을 운영자가 9/7-11/1 로 명시 — season-config `periodLabel`(9/9 – 11/1)과
     불일치. 이 트리는 운영자 문자열을 따름; 북클럽 실사이트 표기 정정은 별도 지시 대기

- 2026-08-07 | 라운드 81 (운영자): **북클럽으로 나가는 링크는 전부 새 탭**.
  · 내비 '레이지데이 북클럽' + 기수 포스터 4·3·2·1기 = 6개 링크 모두
    `target="_blank" rel="noopener noreferrer"`
  · 기수 포스터 링크를 상대 경로에서 절대 URL로: 4기 → `BOOKCLUB_URL`,
    3·2·1기 → `BOOKCLUB_BOOK_URL`(`/#book`). 상수는 Shell.tsx 단일 출처
  · 우측 섹션 제목 '레이지데이 북클럽'도 죽은 앵커(`#shop`) → 북클럽 새 탭
    (운영자 "포스터 섹션"에 그 머리글까지 포함되는 것으로 해석)
  · ⚠️ 남은 항목: **아카이브 캐러셀의 표지 16장**은 아직 상대 `/#book` —
    lazy-club.com 에서는 미들웨어가 랜딩으로 되돌려 도달 불가. 지시 범위 밖이라
    이번엔 손대지 않음, 운영자 확인 후 같은 방식으로 교체 예정

- 2026-08-07 | 라운드 80 (운영자, 라운드 79 ①의 재해석):
  "로고를 두 번째 줄까지" = 걸치기가 아니라 **1행부터 2행까지 꽉 차게 크기를 키우라**는 뜻.
  · 로고 실측 **45px**(모바일 41px) — 두 행 높이(아이콘 16 + row-gap 9 + 링크 20)와 동일.
    헤더 높이는 65px(모바일 61)로 라운드 79와 같다 (로고가 행 높이를 키우지 않음)
  · ⚠️ 함정: 그리드 아이템으로 두고 `height:100%` 를 주면 **로고 높이가 행 높이를 다시
    키우는 순환**이 생겨 헤더가 89px까지 부풀고 모바일에서 본문을 덮었다 →
    `position:absolute` + `top/bottom:10px` 로 흐름에서 빼서 헤더 콘텐츠 상자에 맞춘다
  · 좌측 링크 묶음은 그리드 2열 시작(≈99px)이 로고와 동떨어져 보인다는 지적 →
    1열로 되돌리고 `padding-left: calc(var(--navLogoSize) + 15px)` 로 **로고 바로 옆**에.
    실측 간격 데스크톱 15px·모바일 12px, 좌·우 묶음 하단 정렬 차이 0px 유지

- 2026-08-07 | 라운드 79 (운영자):
  ① **로고가 내비 1~2행을 모두 차지** — `.navLogo` grid-area 1/1/3/2 (세로 중앙),
     2행 좌측 묶음(.navAll)은 로고 오른쪽 **2열부터** 시작. 좌·우 묶음은 같은 2행이라
     하단 정렬 일치 (측정: 데스크톱·모바일 모두 bottom 차이 0px). 헤더 85→65px(모바일 61)
     로 낮아졌지만 본문 여백은 그대로 두어 겹침 없음.
     · 운영자 메시지의 "모임 제품 아카이브 세 가지"는 구 화면 스크린샷 기준 서술로
       판단 — **항목 구성은 라운드 78 확정(모임·아카이브)을 유지**. 제품을 다시 올리려면
       별도 지시 필요
  ② **유휴 60초 셔플을 셸 오버레이로 승격** (`IdleShuffle.tsx` + `idle-shuffle.module.css`,
     WorkroomShell 마운트) — lazy-club.com 트리 **전 페이지 공통**. 페이지 이동이 아니라
     현재 페이지 위를 종이색(var(--paper))으로 덮는 z-200 오버레이이고, 터치·클릭·키·휠·
     스크롤은 물론 **마우스 이동(pointermove)** 까지 동작으로 감지해 즉시 걷힌다 (원래
     화면 상태 보존). 랜딩의 페이지 내 유휴 로직(라운드 77)은 제거·이관 — ComingSoonMain은
     인트로 1회 재생만. 폰트는 같은 둥근모 data URI를 오버레이 CSS에 자체 보유
     (랜딩 CSS import 금지 원칙 준수, 같은 family·src라 브라우저가 중복 제거).
     검증용 ?idle=<ms> 유지, ?t=·reduced-motion 에선 비활성
  ③ **내비 '레이지데이 북클럽' → `https://www.lazyday-bookclub.com` 절대 URL** —
     lazy-club.com 위에서 상대 `/lazyday` 는 미들웨어가 랜딩으로 되돌려 도달 불가했음

- 2026-08-07 | 라운드 78 (운영자):
  ① 내비 **'전체상품' → '모임'** 으로 개명 (링크 대상은 그대로 기획안 홈)
  ② **'제품' 내비 항목 제거** — 내비는 좌 모임·아카이브 / 우 레이지데이 북클럽 3개
  ③ **굿즈(제품) 섹션 잠정 보류** — 라운드 77에서 좌측 모임 아래로 옮긴 블록을
     `WorkroomHome.tsx`의 `SHOW_GOODS = false` 로만 껐다. 구현·CSS·goods-config·
     굿즈 상세 라우트는 전부 보존 — 해제는 `true` 한 줄. (보류는 거절이 아니라 연기)
     결과 홈 = 좌 모임(원데이토크 2건) / 우 레이지데이 북클럽(4·3·2·1기) → 아카이브
  ④ 『시지프 신화』 썸네일 교체 — 범용 `oneday-talk.webp`(One Day Talk 포스터) →
     **`oneday-sisyphus.webp`**(책+설명 카드). 브람스와 같은 카드 문법으로 정렬
  ⑤ **로고 클릭 시 인트로 재생 금지** — 내비 로고가 `/coming-soon?still=1` 을
     가리키고, `still` 이 있으면 인트로 rAF를 건너뛰고 **최종 정지 화면부터** 연다.
     이전 색을 저장·복원하지 않고 이번 시드로 새로 뽑는다 (상태 없음 = 실패 지점 없음,
     운영자 "오류가 덜 발생하며 효율적인 대로"). 유휴 60초 셔플은 이 경로에서도 작동.
     주소창 직접 진입(`/coming-soon`)은 종전대로 인트로 전체 재생

- 2026-08-07 | 라운드 77 (운영자, 7건):
  ① **내비 로고를 원형 마크로 교체 + 10초 1회전 무한 회전** —
     운영자 제공 `lazyclub_logo_circle.png` 3240² → 트림 후 256²,
     10.5KB `nav-logo-circle.png`. `@keyframes logoSpin` 10s linear infinite
     (reduced-motion 시 정지). transform은 레이아웃에 영향 없어 헤더 85px 유지
  ② 모임 목록에서 **랜딩 부연설명 4건 제거**(about·process·place·reviews) —
     원데이토크 2건만 남김. `LANDING_DOCS` 데이터는 **보존**(미렌더)
  ③④⑤ 홈 레이아웃 재배치: **상단 = 좌 모임 / 우 레이지데이 북클럽**,
     **굿즈는 좌측 모임 아래**, **아카이브 캐러셀은 맨 아래**.
     굿즈 진열은 사이드바 캐러셀(`shopItem`+`goodsScaled`) → 모임과 같은
     2열 리스트 문법(`meetingsList`+`item`)으로 통일, 품명만 SUIT(`shopName`) 유지
  ⑥ 굿즈 태그 **어패럴·테이블웨어 폐기 → 전 품목 "제품"** (`goods-config.ts`
     `cat: "제품"` 단일 리터럴 — 홈·굿즈 상세 동시 반영)
  ⑦ **레이지데이 북클럽 1기 추가** — 운영자 제공 포스터 → `poster-1st.webp`
     800×1000 (기존 2·3기와 동일 규격), sold out. 진열 순서 4→3→2→1기
  ⑧ **유휴 60초 → 끝없는 난수 셔플** (랜딩 `/coming-soon`): 마지막 입력에서
     60초 지나면 고정 배열을 벗어나 인트로와 같은 3색 셔플로 무한 회귀.
     이 동안 빙고 써클·맥동·링크 모두 해제(마크가 LAZY·CLUB을 말하지 않으므로).
     아무 입력이나 들어오면 즉시 고정 복귀 + 타이머 재시작.
     검증용 `?idle=<ms>` 오버라이드, `?t=` 고정 모드·reduced-motion에선 비활성
  · 굿즈 섹션 제목은 운영자 표현 그대로 "굿즈" (태그만 "제품") — 내비의
    '제품' 항목은 여전히 `/meetings`를 가리킴(라운드 75 잔재, 별도 지시 대기)

- 2026-08-07 | 라운드 76 (운영자): 내비 미세 조정 —
  ① 로고 노출 높이 27 → **36px**(모바일 22 → 28px)
  ② 2행 재배치: **좌 = 전체상품·제품·아카이브**, **우 = 레이지데이 북클럽**만.
  라운드 75의 "저장소"는 **아카이브**로 개명(라우트 `/archive`·`archive-config.ts`는
  그대로 — 표기만 변경). 좌측 그룹은 `.navAll`이 flex 컨테이너가 되어
  gap 15px(모바일 10px), 우측 `.navMenu`는 9/16 → 끝으로 이동.
  · 로고가 커지며 헤더 높이 데스크톱 76 → **85px**(모바일 69 → 73px) —
    `.content` padding-top 100px·모바일 92px, 랜딩 `.main` 96px는 그대로 충분
    (측정: 5개 페이지 × 2뷰포트 전부 겹침 없음)

- 2026-08-07 | 라운드 75 (운영자): **탑 내비 2행 구조로 전면 교체** —
  라운드 39의 `NAV_ITEMS`(LazyClub 워드마크 + LazydayBookclub·OneDayTalk) 폐기.
  1행 = 로고(좌, 클릭 시 랜딩 인트로) + **계정·카트·검색 아이콘**(우),
  2행 = 전체상품(좌, 기획안 홈) + 레이지데이 북클럽·제품·저장소(우).
  모든 항목 같은 색·같은 서체(SUIT) — 크기·굵기 위계 없음.
  · 로고: 드라이브 `lazyclub_logo.png` 3240² → 여백 트림 후 205×120,
    7.2KB `nav-logo-lazyclub.png` (노출 높이 데스크톱 27px·모바일 22px)
  · 아이콘: noahny.kr 골격 참고해 **자체 드로잉 SVG** 3종 (16px, currentColor,
    strokeWidth 1.2 — 푸터 SNS와 같은 규칙). 카트만 `/cart` 이동, 계정·검색은
    `useToast` 준비중 안내
  · **저장소 신설**: `BASE/archive` + `archive-config.ts`(단일 출처, 현재 빈 배열
    → "아직 쌓인 기록이 없습니다"). 목록 문법은 meetings 재사용, 가격·상태·카트 없음
  · ⚠️ 헤더가 1행(40px)→2행(데스크톱 76·모바일 69)이 되며 **고정 헤더 뒤로
    본문이 들어가는 문제** 발생 → `.content` padding-top 39→100px(모바일 92),
    랜딩 `.main` padding-top 64→96px·그리드 높이식 `100vh-320` → `-360`.
    측정으로 5개 페이지 전부 미충돌 확인
  · 인트로 규율 유지: t=0부터 렌더 + `chromeVeiled`(텍스트·아이콘 배경색,
    로고 opacity 0, pointer-events none), 시작·종료 그리드 박스 완전 동일
  · 모바일도 같은 2행 유지 (햄버거 도입 금지 — 기존 menuTrigger 규칙은 미사용 상태로 존치)

- 2026-08-07 | 라운드 74 (운영자): 푸터 **이용약관을 우측 끝으로** — 사업자정보
  블록(8/11) 안에 있던 것을 푸터 그리드 마지막 칸(15/16, 1행)으로 옮겨
  우측정렬. 좌측 끝 로고와 마주보는 배치. 연락처 블록은 11/16 → 11/15 로
  줄여 겹침 방지. 모바일(4컬럼)은 4/5 — 우상단 코너. 셸 공용이라 이 트리의
  모든 페이지(랜딩·기획안 홈·모임·카트)에 함께 적용

- 2026-08-07 | 라운드 73 (운영자 "애니메이션 그라데이션이 끊겨"): 맥동 시작
  방향을 `alternate-reverse` 로 — 인트로가 끝난 순간 화면은 100% 밝기인데
  순방향 첫 프레임은 25%라 뚝 끊겼다. 역방향 시작이면 100%에서 그대로
  이어받아 **옅어지는 것부터** 시작 (글자·써클 동일 적용)

- 2026-08-07 | 라운드 72 (운영자 "써클 그림자가 나왔다-사라졌다 반복"):
  원인은 71의 구조 — opacity+drop-shadow 는 그림자가 획 밝기에 **곱해져**
  어두운 구간에서 통째로 소멸(이진 반복처럼 보임). 써클만 분리:
  획 깜빡임은 `border-color` α .25↔1, 그림자는 `box-shadow` α .1↔.5 —
  그림자가 획과 무관하게 **점진적으로** 진해진다. hover 고정값도 동일 구조.
  글자는 기존(opacity+drop-shadow) 유지 — 면이라 곱해져도 자연스러움

- 2026-08-07 | 라운드 71 (운영자): ① **써클도 글자와 완전히 동일하게 깜빡임** —
  opacity .25↔1, 동일 위상 (70의 "써클 획 불투명 유지" 폐기) ② **그림자를
  더 크고 조금 더 넓게** — 오프셋 2→3px·블러 3→6px, 최대 α 글자 .16→.3 /
  써클 .45→.5. hover 최대 고정도 같은 값으로 갱신

- 2026-08-07 | 라운드 70 (운영자 정정 2건): ① **hover = 일시정지가 아니라
  최대 상태 고정** — 글자 100% + 그림자 최대(글자 .16 / 써클 .45)가 즉시 뜬다
  (`animation: none` + 정적 값. 69의 "그 밝기에서 멈춤" 폐기)
  ② **써클 그림자가 맥동 중 안 보임** — 3px 얇은 획이라 낮은 α가 묻힌다.
  바닥값 .1을 깔고 최대 .36→.45 로 상향 (사이클 내내 존재감 유지)

- 2026-08-07 | 라운드 69 (운영자): 맥동이 **밝아질 때 hover 그림자도 함께** —
  글자(최대 α .16)와 **써클(최대 α .36)**이 같은 리듬으로 그림자를 얻으며
  떠올랐다 가라앉는다. 키프레임에 filter(drop-shadow α 0↔최대)를 합성,
  써클에도 blinkCap 적용(써클 자체는 불투명 유지, 그림자만 맥동).
  hover 는 밝기·그림자 모두 그 값에서 일시정지. 기존 정적 hover·모바일
  상시 그림자 규칙은 reduced-motion(애니메이션 없음)에서의 폴백으로 존치

- 2026-08-07 | 라운드 68 (운영자): 맥동은 **7글자 동일 위상** — LAZY·CLUB이
  한 몸처럼 같이 밝아지고 같이 어두워진다. 67의 이웃 엇갈림(hotAlt·
  alternate-reverse) 폐기, `blink 1s infinite linear alternate` 로 왕복
  (기점 스냅 없이 부드러운 되돌림, 왕복 2s). hover 일시정지·최저 25% 유지

- 2026-08-07 | 라운드 67 (운영자 "점멸 형태가 레퍼런스랑 다른데?"):
  minguhong.fyi CSS를 실측해 **원문 문법 그대로 교정** — 이진 on/off(66)가
  아니라 `@keyframes blink { 0% {opacity:.25} 100% {opacity:1} }` +
  `1s infinite linear`, 이웃 글자는 `alternate-reverse` 로 위상 엇갈림
  ((r+c) 홀수 칸에 hotAlt), hover 는 제거가 아니라 **그 밝기에서 일시정지**
  (`animation-play-state: paused`). 글자가 완전히 사라지는 순간이 없다.
  교훈: 레퍼런스는 기억으로 흉내 내지 말고 CSS를 직접 열어 볼 것

- 2026-08-07 | 라운드 66 (운영자, 레퍼런스 minguhong.fyi): ① **LAZY·CLUB 7글자
  점멸** — 인트로가 끝나 마크가 링크가 된 순간부터 터미널 커서처럼 이진
  on/off (보임 0.6s / 사라짐 0.45s, steps(1) — 페이드 없음). 눌 수 있음을
  깜빡임으로 유도. 써클·나머지 9글자는 불변. **hover 시 점멸 정지 + 표시
  고정** (잡으려는 순간 사라지면 안 됨), reduced-motion 은 점멸 없음.
  인트로 중엔 .gridLink 가 없어 구조적으로 미적용. CSS 키프레임 사용 —
  단일 rAF 클록 원칙은 인트로 구간 규율이고, 종료 후 반복 점멸은 동기화
  문제가 없다 ② **폰트 data URI 인라인** — 5.1KB 서브셋을 CSS에 base64로
  내장해 네트워크 요청 자체를 제거 (느린 회선의 잔여 빈 프레임도 불가능).
  /public/fonts 파일·preload 링크 삭제

- 2026-08-07 | 라운드 65 (운영자 "첫 로딩에 폰트가 잠깐 안 먹는 순간"):
  **둥근모꼴 자체 호스팅 + 서브셋**으로 FOUT 제거. 원인은 jsDelivr CDN의
  **1.6MB woff**(한글 글리프 전체)를 `font-display: swap` 으로 부르던 것 —
  대체 고딕이 먼저 그려졌다가 교체됐다. A~Z·0~9만 남겨 woff2로 서브셋하니
  **5.1KB**(1/315). `/public/fonts/dunggeunmo-latin.woff2` 로 같은 출처 제공 +
  page.tsx 에서 `<link rel=preload as=font crossOrigin>` + `font-display: block`.
  (같은 출처라도 폰트는 CORS 모드라 crossOrigin 없으면 preload가 두 번 받는다)
  서브셋 도구: `python3 -m fontTools.subset --unicodes=U+0041-005A,… --flavor=woff2`

- 2026-08-07 | 라운드 64 (운영자 "모바일에서도 클릭은 되어야지"): **마크 링크가
  lazy-club.com에서 제자리로 돌아오던 문제** 수정. 원인은 클릭 자체가 아니라
  미들웨어 — lazy-club.com은 `/api` 외 **모든 경로를 랜딩으로 rewrite** 했기에
  마크를 눌러 `/preview/lazyclub-…` 로 가도 다시 랜딩이 떴다(데스크톱도 동일).
  ① 미들웨어에 예외 추가: `/preview/lazyclub-4b073000ddec094f*` 는 내부
  `/lazyday/preview/…` 로 rewrite 해 기획안 홈·모임·카트가 열리게,
  `/lazyday/preview/lazyclub-…` 직접 요청은 통과 ② `useBasePath` 의
  깔끔한-경로 호스트 목록에 lazy-club.com 추가 → 링크가 `/preview/…` 로 렌더.
  검증: Host 헤더 시뮬로 `/`·`/meetings` = 랜딩, `/preview/lazyclub-…` = 기획안 홈,
  모바일 터치 탭으로 실제 이동 확인

- 2026-08-07 | 라운드 63 (운영자): **글자 그림자만** 퍼짐(블러 3px)은 유지하고
  진하기를 라운드 61 수준으로 되돌림 (.3 → .16, press .36 → .2,
  모바일 상시 .22 → .11). 써클은 라운드 62 값(.36/블러 3px) 유지

- 2026-08-07 | 라운드 62 (운영자 "너무 멀리 퍼지고 흐리멍텅"): 그림자를
  **덜 퍼지고 더 진하게** — 블러 7px→3px, 알파 .2→.36 (글자 .16→.3).
  press는 블러 1px·알파 .42. 모바일 상시도 블러 2px·알파 .26(글자 .22).
  교훈: 이 화면에선 넓은 블러가 "떠 있다"가 아니라 "흐리다"로 읽힌다

- 2026-08-07 | 라운드 61 (운영자): hover 시 **써클 안 7글자(C·L·U·B + A·Z·Y)도
  써클과 같은 그림자**를 받아 한 덩어리로 떠오른다. 나머지 9글자(WELCOME TO)는
  그대로. 글자는 면적이 작아 같은 값이면 더 진해 보이므로 알파를 한 단계 낮춤
  (써클 .2 / 글자 .16, 모바일 상시 .14 / .11). 구현은 HOT 칸에 `hot` 클래스

- 2026-08-06 | 라운드 60 (운영자): 써클 완성 후 유지 시간 **1.4s → 1.0s**.
  종료 5.0s → **4.6s** (웰컴 1.0 · 셔플 2.0 · 고정 3.0 · LAZY 3.3 ·
  CLUB 3.6 · 크롬 노출 4.6)

- 2026-08-06 | 라운드 59 (운영자): **빙고 써클 4스텝 와이프 폐기** — 예전처럼
  각각 **한 번에 짠** 나타난다. 간격은 0.25s → **0.3s**: 글자 확정 3.0s →
  LAZY 3.3s → CLUB 3.6s → 완성 화면 1.4s 유지 → 5.0s 크롬 노출 (총 5.0s 불변).
  라운드 56에서 넣은 clip-path 스텝 로직은 제거 (그리드 광학 중심은 유지)

- 2026-08-06 | 라운드 58 (운영자 선택): **마크 전체가 레이지클럽 홈 링크**
  (E안 채택). 선택 이유는 운영자 표현대로 "**변하지 않기 때문**" — 써클에
  굵기·색·점멸을 얹은 A·B·C안은 "정답 표시에 버튼 옷을 입힌" 꼴이라
  조잡했고, 세로 열을 PLAY로 바꾸는 D안은 마크 자체가 변한다. 표시는
  아무것도 더하지 않고 **반응만 그림자로** 준다:
  hover `drop-shadow(0 3px 7px rgba(26,18,8,.2))` · press 짧게 ·
  모바일(hover:none)은 더 옅게 **상시**(0 2px 5px, .14) — 획은 불변.
  ⚠️ 오프셋을 한쪽으로 크게 주면(3px 4px) 획이 두 겹으로 보여 부자연스럽다
  → 오프셋 0 + 블러 위주로. box-shadow 금지(속 빈 써클 안쪽까지 번짐).
  알파벳 밑줄은 `.main .gridLink` 특정도로 `.page a:hover` 규칙을 눌러 제거.
  임시 시안 라우트 intro-a~e·intro-variants 전부 삭제

- 2026-08-06 | 라운드 57 (운영자): **입력이 인트로를 끊지 않는다.** 터치·클릭·
  키·휠·스크롤이 들어오면 애니메이션을 종료하는 대신 **내비·푸터만 먼저
  노출**하고 연출은 5.0s까지 끝까지 재생한다 (라운드 47의 "어떤 입력이든 즉시
  최종 상태" 폐기 — 터치 한 번에 연출이 통째로 날아갈 이유가 없다).
  reduced-motion만 예외로 즉시 최종. 구현: `chromeEarly` 상태를 따로 두고
  `chromeHidden = !done && !chromeEarly`

- 2026-08-06 | 라운드 56 (완성도 다듬기, 시안 검토 후 채택): ① **빙고 써클
  4스텝 와이프** — 가로는 왼→오, 세로는 위→아래로 4토막(60ms 간격, 총 0.24s).
  펜 드로잉(stroke-dashoffset 연속 이징)은 **운영자 지적대로 폐기** — 이 화면은
  셔플·고정·크롬 노출이 전부 이산적으로 끊기는데 유일한 부드러운 요소가 되어
  겉돈다. CSS 트랜지션 없이 stateAt이 스텝 정수를 계산하고 clip-path 값만 교체
  ② **최종 그리드 광학 중심** — 본문 하단 여백 +6vh(센터링이 절반 흡수 → 실제
  약 3vh 상승). 390에서 그리드 상단 91px → 65px, 헤더 하단 40px과 미충돌.
  1280은 상단 여백이 지배해 변화 없음. 절차: 임시 라우트 `/coming-soon-r56`
  으로 시안 배포 → 운영자 승인 → 실 랜딩 이식 + 임시 라우트 삭제.
  같이 검토했으나 **보류**: 주변 9글자 잉크 통일 / 푸터 브랜드 문단 제거 /
  내비 밑줄 제거 / 웰컴 X 글자 위계 낮추기

- 2026-08-06 | 라운드 55 (운영자 정정): 빙고 써클 두 개는 **글자 배열이
  완성된 순간부터 0.25s 간격**으로 채워진다 — LAZY 3.25s · CLUB 3.5s.
  라운드 50의 "완성 배열 1.25s"는 첫 써클까지의 공백으로 체감돼 폐기하고,
  그 시간은 **써클까지 그려진 완성 화면을 유지하는 쪽**으로 옮김
  (3.5–5.0s, 1.5s). 총 길이 5.0s 불변

- 2026-08-06 | 라운드 54 (운영자): 오프닝 잉크 단색(WELCOME TO) 구간
  **0.5s → 1.0s**. 뒤 구간은 통째로 밀림 — 셔플 1.0–3.0s(길이 2.0s 유지),
  고정 3.0s, LAZY 써클 4.25s, CLUB 써클 4.5s, 종료 5.0s (총 4.5s → 5.0s).
  스토리 영상도 같은 타임라인으로 재생성 (인트로 5.0s + 최종 유지 5s = 10s)

- 2026-08-06 | 라운드 53 (운영자): 오픈그래프 이미지 재교체 —
  `og-lazyclub-v4.png` (드라이브 새 원본 3600×1890 → 1200×630, 16KB).
  52 대비 여백·글자 크기가 조정된 버전. 파일명 버전 상향 관례 유지

- 2026-08-06 | 라운드 52 (운영자): 오픈그래프 이미지를 새 원본
  (드라이브 3600×1890)으로 교체 — `og-lazyclub-v3.png` 1200×630, 16KB.
  3600×1890 = 1.9048 로 **OG 권장비 1.91:1과 정확히 일치**해 잘림 없음.
  교체 시 파일명 버전을 올리는 관례 유지 (SNS·CDN URL 캐시)

- 2026-08-06 | 라운드 51 (운영자): 오픈그래프 제목을
  **"레이지클럽  |  lazy-club.com"** 로 (표기·공백 그대로). 브라우저 탭
  제목(`title`)은 "레이지클럽" 유지. OG 이미지는 재확인 결과 라운드 49에
  배포한 `og-lazyclub-v2.png` 와 **원본 파일이 동일**(62,800B, 해시 일치)해
  교체 불필요

- 2026-08-06 | 라운드 50 (운영자): 완성 배열(CWEL/LAZY/UCOM/BETO) 단독 노출
  **0.75s → 1.25s**. 써클은 그만큼 뒤로 (LAZY 3.75s · CLUB 4.0s, 간격 0.25s
  유지), 총 길이 4.0s → **4.5s**. 다른 구간(웰컴 0.5s·셔플 2.0s·마지막 유지
  0.5s)과 색 규칙은 그대로

- 2026-08-06 | 라운드 49 (운영자 지시 3건): ① **완성 배열(CWEL/LAZY/UCOM/BETO)
  단독 노출 0.25s → 0.75s** — 써클은 그만큼 늦게 (LAZY 3.25s · CLUB 3.5s,
  간격 0.25s 유지), 총 길이 3.5s→**4.0s** ② **첫 로딩 시 상단에 프리뷰 이동
  바가 번쩍이던 문제 해결** — `usePreviewBarHide()`는 하이드레이션 뒤에나
  돌아서 첫 페인트에 바(짙은 색)와 34px 스페이서가 노출됐고 레이아웃까지
  밀렸다. 랜딩 CSS 모듈에 `:global(div[class*="previewBar"]) { display:none }`
  을 넣어 **첫 페인트부터 CSS로 숨긴다** (이 모듈은 랜딩 라우트에서만
  로드 — 다른 프리뷰 페이지의 바는 그대로) ③ **이미지 2건 교체**
  (운영자 드라이브 제공): 오픈그래프 `og-lazyclub-v2.png`
  (3000×1350 → 1200×540, 15KB) · 푸터 로고 `logo-lazyclub-v2.png`
  (3240×4050 → 400×500, 11KB). **새 파일명으로 교체** — SNS·CDN이 기존
  OG URL을 캐시하고 있어 같은 이름으로 덮으면 갱신이 늦다.
  좌측 하단 로고의 인트로 중 투명 처리는 라운드 48 구현(`chromeVeiled`
  .footerLogo opacity 0)이 그대로 적용

- 2026-08-06 | 라운드 48 (47 개정, 운영자 프롬프트): 랜딩 인트로 4가지 개정.
  ① **앞에 0.5s 웰컴 정지 화면** — WELX/COME/TOXX/XXXX, 잉크 단색(#1a1208),
  모션 없음 ② **최종 배열 교체** CWEL/LAZY/UCOM/BETO — 2행 LAZY·1열 CLUB
  구조는 유지하고 나머지 9칸이 행 순서로 W E L C O M E T O 가 되게 재배치
  (오프닝 문구가 그리드에 흩어져 남는 구조) ③ **타이밍**: 셔플 2.5s→**2.0s**
  (0.5–2.5s), 고정 2.5s, 빙고 써클은 글자 확정과 동시가 아니라 **0.25s씩 텀**
  — LAZY 2.75s · CLUB 3.0s, 3.5s 종료(총 길이 유지) ④ **내비·푸터를 t=0부터
  최종 레이아웃 그대로 렌더**하고 인트로 동안만 배경색으로 가린다
  (`chromeVeiled` 신규 클래스: color=--paper, pointer-events none,
  border-top 투명, 로고 img는 opacity 0). 인트로 전용 fixed 오버레이
  `.introRoot` 폐기 → **시작·종료 시점 레이아웃이 완전히 동일**.
  상단 고정 헤더에 그리드 윗줄이 잘리던 문제도 여기서 해결
  (본문 padding-top 64px + 그리드 높이를 `(100vh-320px)/1.273`로 산정 —
  390/1280 모두 헤더 아래 여유 확보). **스크롤 잠금은 넣지 않는다** —
  잠그면 데스크톱 스크롤바가 사라지며 폭이 바뀌어 배치가 흔들린다
  (스크롤·휠은 스킵 트리거라 인트로가 즉시 끝남). 4컬러(잉크 포함) 셔플
  안은 운영자 지시로 폐기 — 색 규칙은 라운드 47 확정본 유지.
  검증: 390·1280에서 t=300/1200/2600/2800/3100/9000 전 구간 그리드
  박스 좌표·문서 높이 불변 확인, 단색 통일·주변색 겹침 0 (6회 반복)

- 2026-08-06 | 라운드 47 (운영자 최종 프롬프트 — 개념 전환): lazy-club.com은
  커밍순이 아니라 **정식 랜딩페이지 + 인트로**. ① COMING SOON 문구·CLI
  타이핑·흑백 반전(invert/chromeDim/2-레이어 와이프)·커서 크롤 전부 폐기,
  써클은 3px 1단계 즉시 표시 ② 시퀀스 총 3.5s 1회성: 0–2.5s 4×4 셔플만
  화면에 단독(내비·푸터 미렌더) — 16칸 각자 60~140ms 간격으로 A–Z 교체,
  글자 바뀔 때마다 3색 재배정 → 2.5s 고정(CDEF/LAZY/UVWX/BCDE, LAZY·CLUB
  7글자는 무작위 1색 통일 — 새로고침마다 다름) → 2.5s LAZY 써클 →
  2.75s CLUB 써클 → 3.0–3.5s 유지 → 3.5s 내비·푸터 즉시 출현 ③ 배경
  #f8f3ef 시작~끝 불변 (Shell `paper` prop으로 페이지 한정 --paper 재정의)
  ④ 스킵: 어떤 입력이든·reduced-motion이면 즉시 최종 상태, 방문마다 재생
  ⑤ 구현: 단일 rAF + 순수 stateAt 유지, 난수는 마운트 시 시드 1개
  (시드 해시 `rnd(seed,a,b)`로 셔플 글자·색·간격·단색 전부 유도 —
  프레임별 Math.random 금지), ?t=<ms> 디버그 유지 ⑥ 경로 /coming-soon은
  내부 식별자로만 존치(미들웨어·OG 바인딩), 명칭은 '랜딩 인트로'
  ⑦ 추가 지시: **최종 고정 상태에서 써클 주위 글자 색은 써클 안 단색과
  반드시 달라야 함** — 셔플 마지막 색이 단색과 같으면 나머지 2색 중
  하나로 결정적 치환. 부수: usePreviewBarHide()를 Shell에서 export
  (인트로가 셸 밖에서 렌더되므로 자체 호출)

- 2026-08-06 | 라운드 46 (45 보완, 운영자 "배경과 텍스트가 하나씩이 아니라
  동시에 반전"): 색상 크로스페이드 폐기 — 중간 혼색에서 글자·배경이 만나
  순차 반전처럼 보임. **반전 팔레트 레이어를 위에 겹치고 clip-path 와이프
  (위→아래, 1.2s)로 걷어내는 2-레이어 방식**으로 교체: 경계면이 지나는
  자리마다 배경·텍스트가 같은 순간에 원래 색으로 반전. backdrop-filter
  안(1차 시도)은 청색 시프트·써클/내비 비침으로 폐기. 함정 2건: ① 셸을
  두 겹 렌더하면 프리뷰 바 숨김 cleanup이 서로를 깨뜨림 → 참조 카운트
  ② 반전 레이어의 fixed 헤더는 translateZ(0)로 레이어에 가둠

- 2026-08-06 | 라운드 45: coming soon 최종 리듬 — ① 총 **~7s**로 단축
  (타이핑 1.6s·크롤 보폭 0.35s) ② 끝의 **깜빡임·블록 색칠 단계 전부
  제거** ③ 마지막 글자 완성(4.5s) 후 **0.7s 텀 → 색상이 서서히 반전**
  (CSS 1.5s 페이드, 팔레트 변수 전환 + `pageSmooth` 트랜지션) ④ **빙고
  써클은 처음부터 끝까지 고정 흑색 3px** — 반전 배경에선 안 보이다가
  배경이 밝아지며 서서히 드러남 (thin→thick 단계 폐기). 1회성 유지

- 2026-08-06 | 라운드 44 (43 정정): 반전은 **페이지 전체**로 되돌리고
  (43의 본문 한정은 섹션 바가 블록으로 도드라져 폐기), 내비·푸터는
  **글자만 원래 잉크색으로 고정**해 반전 배경에 묻히게 한다 (운영자
  의도: "글자만 반전하지 말자 — 그래야 눈에 안 띈다"). 구현은 Shell의
  `invert` + `chromeDim` 클래스 — 서브트리 안에서 팔레트 변수를 잉크로
  되돌려 뒤에 정의된 규칙(.footerDesc p 등)까지 함께 묻힌다

- 2026-08-06 | 라운드 43 (폐기, 44로 대체): 반전 범위를 본문 영역으로 한정

- 2026-08-06 | 라운드 42 (41 정정): 반전 방향 뒤집기 — **처음부터 반전
  (잉크 배경)으로 시작**해 타이핑·크롤·써클 전 시퀀스가 반전 위에서
  진행되고, 7.9s 블록 강조 후 **9.5s에 원래 오트 컬러로 복귀**(직전
  2프레임 깜빡) → 복귀 상태로 고정 (1회성 유지)

- 2026-08-06 | 라운드 41: coming soon 후반부 **A안(정답 반전) 확정 + 1회성
  전환** — ① 클라이맥스에서 화면 전체 반전(셸 팔레트 `--ink/--paper`
  스왑이라 헤더·푸터까지, 2프레임 깜빡) ② 반전 구간의 찾은 글자는
  **사라지지 않고 블록**으로 표시(크롤 커서와 같은 문법), 나머지 글자도
  옅게 유지 ③ 이후 **역반전**으로 오트 컬러 복귀 ④ **반복 없음 — 역반전
  상태(9.5s)로 고정**. Shell에 `invert` prop 추가(기본 false, 타 페이지
  무영향)

- 2026-08-06 | 라운드 40: ① 원데이토크 상세 이미지 — **책 단독 표지 제외,
  카드 이미지 1장씩만** (브람스·시지프) ② **후반부 임팩트 시안 3안**
  (제안, 채택 대기): `coming-soon-finale` 쇼케이스 — 진단은 "써클 완성
  후 결말 없이 소등되어 사건이 흐지부지". A **정답 반전**(화면 전체
  잉크 반전 + 두 단어만 종이색, 2프레임 깜빡 — 추천) / B **낱말 추출**
  (나머지 9글자 일괄 소멸 → 7글자로 워드마크) / C **스탬프**(두 단어에
  잉크 블록 반전 + 하단 LAZY CLUB 타이핑). 검토용 `?t=<ms>&v=` 시점 고정

- 2026-08-06 | 라운드 39: 공개 전 정리 — ① 내비에서 메뉴 관련 버튼 전부
  숨김: **LazydayBookclub·OneDayTalk 두 항목만** (About·search·login·
  cart·모바일 menu 버튼 제거, 모바일도 두 항목 직접 노출) ② 원데이토크
  목록은 **시지프(모집중, 맨 위)·브람스(sold out) 두 상품만** — 랜딩
  콘텐츠·지난 기수·카테고리 필터 제외 ③ 홈 하단 '레이지데이 북클럽'
  4기~2기 진열은 그대로 유지

- 2026-08-06 | 라운드 38: coming soon 미세 보정 3건 — ① 타이핑을 **CLI식
  좌측 고정**으로 (완성 문구 폭으로 박스를 고정하고 왼쪽에서 한 글자씩
  채움 — 가운데 재정렬 폐기) ② 써클 순서 **LAZY 먼저 → CLUB** ③ 사이클
  **12s → 10.5s** (타이핑 2.0s·보폭 0.38s·유지 ~2s)

- 2026-08-06 | 라운드 37: coming soon **전면 재구성 — '퍼즐 푸는 픽셀 커서'
  연출** ① 점등 순서 변경: **C → L → U·A → B·Z → Y** — 반전 블록 선택
  커서 2개가 그리드를 기어감 (커서1 CLUB 세로, 커서2 두 박자 늦게 A→Z→Y
  가로 — 보폭 겹침이 그룹을 자연 생성, 0.45s/스텝) ② 써클은 단어 완성
  순서로 **CLUB → LAZY** (얇게→굵게 유지) ③ **부자연스러움 원인 해결**:
  타이핑(CSS 키프레임)과 그리드(rAF)가 다른 시계로 돌아 탭 전환 시
  위상이 어긋남 → CSS 애니메이션 전폐, 전체를 단일 rAF 클록의 순수 함수
  (`stateAt`)로 통합. 일괄 소멸·무그라데이션 규칙 유지, 12s 사이클

- 2026-08-06 | 라운드 36: coming soon 점등 순서·속도 확정 — ① 글자는
  **L·C → A → Z·U → Y·B** 4단계(0.25s 간격, 두 단어 짝 리듬)
  ② 글자 완료 후 **LAZY 써클 얇게→굵게 → CLUB 써클 얇게→굵게** 엄격
  순차 (써클·글자 및 써클끼리 겹침 없음) ③ 사이클 **14s → 12s**

- 2026-08-06 | 라운드 35: coming soon 규칙 확정 2건 — ① **사라짐은 전부
  일괄·즉시** (하나씩 지우기·흐려지기 금지: 소등 스태거·중간 밝기 0.55
  폐기, 글자·써클이 12.04s에 동시 소등) ② **그라데이션 금지 — 빙고
  써클은 얇은 선(1px) → 굵은 선(3px) 2단계**로 채움 (LAZY → CLUB 순서).
  구현: ON 캐스케이드 + 일괄 OFF는 CSS 키프레임으로 불가 → 그리드·써클을
  rAF 상태 머신으로 전환 (타이핑·커서는 CSS 유지, 14s 사이클 공유)

- 2026-08-06 | 라운드 34: coming soon **모션 톤 보정** ("너무 빨라, 과격,
  톤앤매너 안 맞아") — 사이클 9s → **14s**. 급점멸 3회·1px 지터 폐기 →
  글자당 0.18s 캐스케이드 + **dim→0.55→1 2단계 점등**(픽셀 LCD 예열
  리듬, 하드 스텝은 유지 = Y2K를 느린 디지털 리듬으로). 캡슐은 단어를
  '찾았다'는 표시처럼 LAZY(+0.9s)→CLUB(+1.5s) 순서 점등. 활성 유지
  ~5s, 소등은 점등의 역순 2단계. 타이핑 2.8s·커서 점멸 1.1s 주기로 감속

- 2026-08-06 | 라운드 33: **coming soon C안(워터마크) 확정 + 폰트 조판 전환**
  ① 마크 PNG 미사용 — 둥근모꼴로 4×4 워드서치 그리드를 **직접 조판**(글자
  개별 제어 가능, 이미지 요청 0) ② 시퀀스 재설계(9s): 타이핑 → 커서 2회
  점멸 → **문구 전체가 한 번에 소멸**(역타이핑 폐기) → 가로 LAZY·세로
  CLUB **활성화**(y2k 스텝 점멸 3회 + 1px 지터, 캡슐 동시 점등, 글자당
  0.06s 스태거) → 유지 → 소등 → 쉼 → 반복. 그라데이션 없음
  ③ 검토용 `?phase=type|hot` 정지. 함정 2건 기록: 캡슐을 그리드 아이템으로
  두면 글자 자동 배치가 밀림(→ 절대 배치), flex 축소가 걸리면 마지막 글자가
  잘림(→ `flex: none`)

- 2026-08-06 | 라운드 32 (제안, 채택 대기): **COMING SOON 배치 3안** —
  `coming-soon-layouts` 쇼케이스. A 폭 맞춤(추천: 텍스트 폭 = 마크 폭,
  실측 보정 나눗수 8.25) / B 밴드 오버레이(마크 가로지르는 종이색 밴드) /
  C 워터마크(마크 16% + 초대형 문구). 검토용 `?still=1&v=` 정지 파라미터.
  ※ 타이핑 폭은 `ch` 계산 → **실측(--typeW) 주입**으로 교체 — 큰 폰트에서
  마지막 N이 잘리던 문제 해결. 채택 시 이 방식으로 /coming-soon 이식

- 2026-08-06 | 라운드 31: ① coming soon 마크 **화면 세로 가득 확대** —
  원본 PNG의 투명 여백을 트림(1000×1273)해 실제 잉크가 박스와 일치하도록
  하고 `height: min(72vh, 120vw)`로 배치 (모바일은 폭 94vw 가득, 웹은
  세로 72vh) ② **푸터는 두 페이지 완전 동일** — coming soon에서도 로고·
  브랜드 문단·사업자정보·연락처·SNS 전 섹션 유지 (라운드 28의 축소 폐기,
  `comingSoon` 셸 분기 제거)

- 2026-08-06 | 라운드 30: **역할 분리 확정** — ① coming soon(마크+둥근모꼴
  타이핑)은 **lazy-club.com 전용**: `/coming-soon` 페이지 신설, 미들웨어가
  lazy-club.com·www의 모든 페이지 요청을 여기로 rewrite (도메인 연결만
  남음) ② 난수 경로 홈은 **기존 기획안(전체 섹션) 복원** — 내부 검토용
  유지 ③ 타이포는 **B안(타이핑) 채택** + 보정: 마지막 N까지 완주(폭
  11ch+자간 0.92em), 커서 2회 점멸 후 역타이핑으로 지우고 반복(6s 사이클)
  ④ 파비콘·OG는 트리 layout 상속으로 coming soon에도 동일 적용

- 2026-08-06 | 라운드 29 (제안, 채택 대기): **Coming Soon 타이포 이펙트
  시안 4종** — 둥근모꼴(DungGeunMo, 눈누 무료) 로드,
  `coming-soon-effects` 쇼케이스 페이지. A 워드서치 서클(추천 — 마크 오벌
  문법 연장) / B 타이핑+블록커서 / C 아케이드 점멸 / D 전광판 마퀴.
  채택 시 coming soon 홈(마크 아래) 이식 + 폰트 서브셋 자체 호스팅 전환

- 2026-08-06 | 라운드 28: **coming soon 모드 전환** ("사업자정보와
  네비게이터만 남기고 모든 건 노출하지 않을 거야. 단순히 이미지만") —
  홈 본문 = 워드서치 이미지 단독(세로 중앙), 푸터 = 사업자정보+연락처
  (법정 표기)만, 로고·브랜드 문단·SNS 미노출. `Shell.tsx`의
  `COMING_SOON` 플래그 하나로 전환 — 오픈 시 false로 되돌리면 전체 복원.
  하위 페이지(모임 목록·상세·카트)는 내비 경유 접근 그대로

- 2026-08-06 | 라운드 27: 운영자 첨부 **워드서치 마크(세로형 1600×2000)를
  최적화해 홈 최상단 게재** — 흰 배경 → 투명 알파, 잉크색(#1a1208) 틴트,
  1000px PNG 20KB. lazy-club.com coming soon 소재. 중앙 정렬 340px(모바일
  72%). ※ lazy-club.com 도메인 연결·호스트 분기는 별도 진행 대기

- 2026-08-04 | 라운드 26: 기수 진열(4·3·2기)을 **구 굿즈와 동일한 배열**로
  통일 ("원래 굿즈 배열했던 것처럼") — shopItem 문법 재사용: 데스크톱
  이미지 3/5+제목 우측 행, 모바일 가로 스와이프+도트. 라운드 24의 세로
  풀폭 스택은 폐기

- 2026-08-04 | 라운드 25: 레이지클럽 OG 이미지 교체 — 랜딩 정본(아이러브
  레이지데이) 대신 **운영자 제공 LazyClub 워드서치 마크**(드라이브 1440px
  → 1200×676 최적화, `home-v3/og-lazyclub.png`). 제목·설명은 라운드 24
  그대로

- 2026-08-04 | 라운드 24: ① 최상단 정규모임 모듈 삭제 ② 섹션명 한글화 —
  books→**아카이브** / meetings→**모임** / shop→**레이지데이 북클럽**
  ③ 구 shop 위치에 기수 진열: 4기(포스터+제목만, →랜딩) + 3기·2기(sold out
  오버레이) — 홈 모임 리스트에서 지난 기수 제거(사이드바로 이동, 목록
  페이지는 유지) ④ 굿즈는 그 아래 포스터 대비 2/3 축소 진열 ⑤ 트리 전용
  메타 신설(layout.tsx): OG 제목 "레이지클럽" / 설명 새 브랜드 문장 /
  이미지 랜딩과 동일한 og-lazyday-heart-v5 ⑥ 파비콘 = 운영자 제공 LC 픽셀
  마크(드라이브 3240px 원본 → 32/192/180 최적화, 이 트리 한정)

- 2026-08-04 | 라운드 23: **공유 방식 전환 — 토큰 링크 대신 실도메인 난수
  경로** ("그냥 프리뷰페이지로 하나 파자 복잡한 하위페이지명으로"). 프리뷰
  트리 전체를 `preview/lazyclub-4b073000ddec094f`로 개명하고 main 병합 →
  `lazyday-bookclub.com/preview/lazyclub-4b073000ddec094f`에서 로그인 없이
  접근. 프리뷰 레이아웃 noindex 유지(검색 비노출), 경로 비추측성으로 보호.
  실사이트 페이지·기존 라우트 불변

- 2026-08-04 | 라운드 22: 홈 상단 모듈 **3열 → 원문 상품 상세형 2열** —
  중간 제목 열 폐지, 텍스트 전부 우측 한 열(11/15)로: 라벨 → 제목 → 설명
  3문단 → (인터뷰 문단 아래) 일정·장소·4기 커리큘럼 필드 → 자세히 보기 →
  저장. 가운데는 workroompress 상품 상세(219886)와 동일한 3칸 공백
  (포스터 1/8 유지)

- 2026-08-04 | 라운드 20~21: ① 푸터 로고 가로세로 2배 확대(44→88px)
  ② 푸터 브랜드 문단을 운영자 원문 **한 문장으로 전체 교체** — "저마다 다른
  삶의 궤적 속 불협화음이 예술의 본질을 관통하며 하나의 선율이 되는 순간을
  믿습니다." (구 두 문장 "결이 맞물리는 사람들과…믿습니다." 전체 대체 —
  main PR #153의 랜딩 메타 설명 교체와 동일 문면·동일 치환)

- 2026-08-04 | 라운드 17~19: ① 상단 모듈 확정형 — 포스터 절반 폭(1/8·94%) /
  중앙 제목 "레이지데이 북클럽 4기"+라벨 "오프라인 독서모임"+일정('26.09.07-
  '26.11.01 격주 5회)·장소·커리큘럼 필드+저장 / 우측 브랜드 설명 3문단+자세히
  보기, 가격 미노출, 3열을 포스터 시작선 정렬, 모듈 하단 전폭 가로 괘선
  ② **이 페이지의 브랜드 = 레이지 클럽(허브)** — 내비 4항목
  LazyClub(홈·워드마크) / LazydayBookclub(기수제 랜딩) / OneDayTalk(목록) /
  About(준비 중), 푸터 로고 = 운영자 제공 투명배경 LAZY/CLUB 픽셀 격자 로고
  ③ 프리뷰 공개 전환 안 함 — 공유는 토큰 링크 방식 유지

- 2026-08-04 | 라운드 16: ① **컬러 오트 베이스 확정**(#f7f3ee/#1a1208/#ece5da),
  임시 팔레트 패널 제거 ② 상단 모듈을 워크룸 **책 상세 3열 구성**으로 재편 —
  좌 포스터(books형 2/5에서 88%로 추가 축소 + 상단 24px 오프셋 = 불균형
  재해석) / 중 라벨 "레이지데이 북클럽 4기"·제목 "타인의 낯선 시선을 기꺼이
  환대하는 이들의 오프라인 독서모임"·'26.09.07-'26.11.01·저장 / 우 장소·
  4기 커리큘럼(5회차)·취소선 가격(₩200,000→₩150,000) ③ 이 상품만 구매·카트
  없음 — **자세히 보기** 단일 버튼·포스터 클릭 → 기존 랜딩 ④ 설명 3문단은
  모듈에서 제거(제목이 흡수). ⚠ 장소 문구 "사당역 10번 출구 도보 3분"은
  운영자 제공 원문 — 기존 별내 주소와 상이, 확인 대기

- 2026-08-04 | 라운드 14: ① 내비 **영문 단일 확정 — Bookclub / One Day Talk /
  Brand** (?nav=ko|en 두 벌 체계 종료, 03의 이중 벌 조항 대체) ② 리스트 항목
  정돈 — 저장 아이콘 위 메타 텍스트("후기"·"일정과 장소"·날짜·"시즌 종료")와
  카테고리 뱃지("· 모집중/마감") 전부 제거, 카테고리 슬러그가 성격을 말함:
  about / process / place / reviews (documents 세분화), 상태는 오버레이만
  ③ 푸터 텍스트 12.6px(12↔13.2 중간)·weight 600 ④ About Lazyday Bookclub
  링크 제거, SNS는 아이콘 행(인스타 + **카카오톡** pf.kakao.com/_gixaAX/chat)

- 2026-08-04 | 라운드 12: 내비 ko 2번째 항목 재명명 — **원데이 클럽 → 원데이
  토크** (콘텐츠 표기 "원데이 토크"와 통일). 목록 페이지 sub·주석 동기화.
  이전 라운드의 "원데이 클럽" 표기는 이 변경으로 대체됨

- 2026-08-04 | 라운드 11: 지난 기수(2기·3기)를 **sold out 상품으로 리스트
  하단 진열** — 카테고리 `bookclub`, 드라이브 포스터 정본(2기 나선·3기 문)
  수급, 메타 "기간 · 시즌 종료", 목록 필터에 bookclub 추가. 포스터 폴더의
  텍스트 페이지(당신의 일기·고독과 고립)는 에세이 자산으로 확인 — 미사용 보류

- 2026-08-04 | 라운드 10: 하위 페이지 1~4순위 일괄 구현 — 공유 셸 추출(Shell.tsx:
  헤더·푸터·토스트·팔레트), 원데이 상세 2종(`meetings/[slug]` — 카드뉴스 원문
  카피, 구매하기→원데이 apply), 굿즈 상세 3종(`shop/[slug]`), 목록
  (`meetings` — 카테고리 필터), 카트(`cart`). **카트·저장은 로그인 없이
  localStorage 실동작**(로그인 DB 세팅 전 임시 — 회원 도입 시 서버 승격),
  주문하기는 결제 연동 전 안내. 내비 '원데이 클럽'은 목록 페이지로 연결
- 2026-08-04 | 상태 3단계 확정: open(모집중) / **soldout(마감)** /
  **upcoming(오픈 예정)** — 마감·예정 상품은 썸네일·상세 이미지에 반투명
  블랙박스 오버레이("sold out"/"coming soon") 마킹, 저장·카트 담기는 계속
  가능. 굿즈 전 품목은 판매 전 = upcoming

- 2026-08-04 | 라운드 9: 상품 모듈 여백을 원문 상세 실측대로 재편 — 이미지
  1/8 · 텍스트 **11/15**(가운데 3컬럼 공백), 제목 **36px/1.278**, 순서
  설명→필드(일시·장소·문의)→가격(₩150,000)→버튼→저장. "반반 레이아웃"
  탈피. 하위 페이지 기획 v1 문서 신설(09-subpages-plan.md)

- 2026-08-04 | 라운드 8: ① home-v3-workroom 페이지에서 프리뷰 이동 바 숨김
  (다른 프리뷰 페이지는 유지), 헤더 top 0 ② **구성요소 전량 노출 원칙** —
  저장(북마크)·login·cart·구매하기·카트 담기를 홈부터 배치, 미구현 기능은
  클릭 시 안내 토스트("~준비 중입니다."). 03의 "기능 없는 메뉴 미노출" 방침
  갱신. 구매하기는 기수제 신청 페이지(/apply)로 실연결

- 2026-08-04 | 라운드 7: ① 푸터 브랜드 문단+About 블록을 사업자 정보 전까지
  가로 확장(컬럼 2/8) ② **임시 팔레트 패널** 추가(시안 검토 전용, 이식 시 제거
  — 배경/텍스트·괘선/보조 회색 3집합 + 프리셋 백지·오트·크림·반전)
  ③ 상품 상세 방향 확정: cart·구매·저장 버튼 + 하위 페이지 필요, 단
  **주소 등 불필요 정보 미수집** — 티켓형 결제(이름·전화만)로 워크룸식
  '상품화 부조화'는 살리되 UX 불편은 제거

- 2026-08-04 | 라운드 6: ① shop 카테고리 'goods' 폐기 → 품목별 소분류
  **apparel / tableware** (운영자 위임 — 'products'보다 워크룸 소분류 문법 부합)
  ② 상품 모듈 첫 문단 원문 원복("문학과 철학, 예술의 한가운데서…")
  ③ 푸터 문단 = "결이 맞물리는 사람들과…" 교체 + **About 링크와 한 블록 통합**
  (문단 위·링크 아래 행, 컬럼 2/5) ④ 이미지 외곽선 **제거 확정**(ON 번복)
  ⑤ 캐러셀 세로 휠 하이재킹 제거 — 페이지 상하 스크롤 유지, 가로 이동은
  드래그·도트·자동 넘김만

- 2026-08-04 | 도서 캐러셀 **자동 넘김 도입** (일정 간격 자동 전환 + 하단 도트 바,
  원문 문법: 간격 2000ms·전환 1000ms·조작 후에도 지속) — 02 v3의 "자동재생
  이연" 조항 해제 (운영자 지시). prefers-reduced-motion에서는 정지

- 2026-08-04 | 카피 미세수정(운영자 원문): documents 헤더 2건 →
  "타인의 낯선 시선을 기꺼이 환대하며" / "한 편의 발제문에서 시작되는 북토크".
  상품 모듈 설명 첫 문단 → "결이 맞물리는 사람들과 철학과 고전을 함께
  읽습니다. 저마다 다른 사유의 궤적 속 불협화음이 고전의 본질을 관통하는
  하나의 선율이 되는 순간을 믿습니다." (푸터 발췌는 05 원문 유지 — 범위 절제)
- 2026-08-04 | 상품 모듈 설명 2·3문단(비슷한 결…/그래서 모든 멤버는…)은
  **모바일(≤720px) 미노출** — 텍스트 높이 조정 (데스크톱은 전체 노출)
- 2026-08-04 | 이미지 미세 외곽선 **확정 ON** — 단 실이미지가 있는 상품·썸네일에
  한함 (플레이스홀더에는 미적용)

- 2026-08-04 | 홈 최상단 = **정규 독서모임 상품 모듈** (워크룸 상품 상세 구성
  차용 — 좌 4기 포스터(드라이브 정본 1.png) / 우 "레이지데이 북클럽 4기"·
  "오프라인 독서모임"·일정 나열·브랜드 3문단 원문). 원문 홈과 달라져도 되는
  지점으로 공인. meetings 리스트의 notice 항목은 제거 — 모집 진입점은 상단
  모듈 1곳 (03 검수 조항 승계)
- 2026-08-04 | 로고 = 원본 3색 풀컬러 사용 + "색을 지나치게 제한할 필요 없다"
  — 02 §1 유채색 UI 금지 완화 (CLAUDE.md §9 로고 규칙도 개편 홈에 한해 갱신)
- 2026-08-04 | 이미지 비율·여백 보정: 썸네일 3:4 → **4:5**(보유 이미지 실비율),
  리스트 열 간격 14→20, 항목 패딩 7.5→12 (가로 비율 높은 이미지의 답답함 보정)
- 2026-08-04 | 이미지 틀 미세 외곽선(1px rgba(26,18,8,.09)) — 시안 기본 ON,
  운영자 미확정(비교 스크린샷으로 판단 대기)

- 2026-08-04 | 홈 서체 배역 확정(라운드 3): 모임 설명 헤더·About 링크 =
  **Gothic A1**(눈누 #891, OFL) "550 정도" → 정적 9굵기 최근접 상위 600 채택 /
  사업자 정보 = Gothic A1 **300**(조금 더 가늘게) / 영문 구성 내비·섹션 라벨·
  카테고리 = **SUIT** / 푸터 브랜드 문단 = Noto Serif KR 명조 유지(라운드 2)
- 2026-08-04 | 이미지 페어 카피는 워크룸 원문 뉘앙스(도록체 명사형·한다체·
  『서명』 앞세움)로 — 05 합쇼체 단일 규정에 대한 **홈 리스트 한정 예외**.
  notice 문구(v1.1-6)는 불변
- 2026-08-04 | 드라이브 폴더 공개 전환 → 굿즈 실사진 3종(코스터·머그 5색·
  **티셔츠 추가**)·4기 포스터·원데이 토크 카드 2종 수급, sharp 최적화 후
  `public/linky-lounge/book-club/home-v3/` 정착 (머그는 시안 하단 텍스트 크롭)
- 2026-08-04 | 내비 ko 확정: **북클럽 / 원데이 클럽 / 브랜드** (미결 해소).
  en은 books / meetings / about 유지, 섹션 라벨은 소문자 영문(books/meetings/shop) 유지
- 2026-08-04 | 홈 이미지 즉시 장착: 캐러셀 실표지 16권(book-config 단일 출처,
  최신 우선) + meetings 리스트 썸네일 1차부터 노출(03의 "1차 미노출" 갱신).
  운영자 드라이브 링크 폴더(1tvsJ10…)는 연결 계정에서 접근 불가 —
  레포 보유 이미지(표지 16·모임 4·후기 4·4기 포스터)로 대체 장착
- 2026-08-04 | 서체 혼용 확정: Pretendard(고딕 — 라벨·메타·본문) +
  Noto Serif KR 500(명조 — 리스트 제목·푸터 브랜드 문단). 랜딩의 고딕·명조
  무게 밸런싱 문법 계승. 조선신명조 등 추가 명조는 필요 시 재론
- 2026-08-04 | 홈 콘텐츠 채움: notice(4기 모집, season-config 파생 메타) +
  booktalk(원데이 2건) + documents 4건(랜딩 섹션 인덱스 — 모임소개·진행방식·
  일정장소·후기, 랜딩 확정 카피 우선·다듬기만 쓰레드 어조)
- 2026-08-04 | 문서군 v3 재정비: 워크룸 이식판으로 전면 갱신. 03 v2(좌축+오프셋+
  신문 구도) 폐기, 07은 콘텐츠 제작 층으로 재배치, 모션 예산 축소·이연 (02 v3)
- 2026-08-04 | 리스트 분류 체계 채택: notice / booktalk / lecture / reading /
  documents (소문자 라벨). one-day-config.ts 스키마에 category 필드.
  플랫폼화(다수 모임장) 확장 구조로 공인
- 2026-08-04 | 개편 접근 전환: workroompress.kr 홈 구조를 충실 이식하고
  레이지데이 콘텐츠로 치환. Claude Design 목업은 콘셉트 증명으로 종료,
  픽셀 충실도는 Code에서 원본 값 추출 + 대조 루프로
- 2026-08-04 | 이식 원칙: 구조·수치는 차용, 메뉴명·콘텐츠·자산은 전부
  레이지데이 고유 (복제 시비 방지). 홈에 발명형 히어로 금지 — 모집은
  notice 항목으로 (원문 문법이자 "홈 신청 금지" 원칙 충족)
- 2026-08-03 | 로고 종결: 리컬러 없음. 화면 mono-ink, 색면 위 mono-cream,
  파비콘·OG 원본 풀컬러. 이 대화 산출 lazyday_logo.svg가 벡터 마스터
- 2026-08-03 | 카피 v1.1 확정 (v3에서 구조 재매핑 — 05 참조)
- 2026-08-03 | 그래픽 에셋 신규 제작 원칙 (시스템 방식, 타인 템플릿 전재 금지)
- 2026-08-03 | 브랜드 가치: "불균형의 균형" + 도가의 자연스러움
- 2026-08-03 | 다색 허용 (단, v3에서 UI는 백지+잉크, 유채색은 콘텐츠 이미지에만)
- 2026-08 | 반응형 3뷰포트(390/768/1280) / 기수 표기 "4기" / 일회성 관리자
  수동 등록(관리자 페이지 보류) / 홈+일회성 먼저, 기수제 상세는 확산 단계

## 미결

- 브랜드 컬러(로고 색 버전) — 보류 지속, 콘텐츠가 쌓인 뒤 재론

## 보류

- 일회성 모임 관리자 페이지 / 기수제 상세 새 문법 적용 시점 /
  마퀴·리빌 등 모션 확대분 (기수제 상세 후보로 이연)
