-- ============================================================================
-- 0009 · 분류 주석 정정 — '기결제자'는 목록에서 빼는 표시가 아니다
-- ============================================================================
-- 0008 은 triage 를 통째로 "목록에서 빼는 이유"라고 적었는데, 운영자가 바로잡았다:
--   "기결제자는 물론 기본제외대상이 아니지" (2026-09-01)
-- 기결제자는 허수가 아니라 **가장 진짜인 접수**다. 오히려 자주 봐야 하는 행이라
-- 빼면 안 된다 — 표시만 하고 기본 목록에는 그대로 남는다.
--
-- 즉 분류에는 두 종류가 있다:
--   · **빼는 표시**  test · typo · dummy · duplicate → 기본 목록에서 빠진다
--   · **그냥 표시**  paid                            → 목록에 남는다
-- 어느 쪽인지는 **애플리케이션이 정한다**(라우트의 HIDDEN 배열). DB 는 여전히
-- 형태만 본다 — 그래야 분류를 늘리거나 성격을 바꿀 때 마이그레이션이 필요 없다.
--
-- ⚠ 컬럼·인덱스·데이터는 건드리지 않는다. **주석만 고친다.**
--   0008 은 이미 적용된 파일이라 수정하지 않는다(append-only) — 그래서 새 파일이다.
-- ⚠ 인덱스 applications_triage_open_idx(where triage is null)는 그대로 둔다.
--   기본 목록 조건이 `triage is null or triage not in (…)` 으로 넓어져 이 인덱스만으로는
--   다 못 받지만, 지금 규모(수백 행)에서 새 인덱스를 더할 이유가 없다.
-- ============================================================================

comment on column public.applications.triage is
  '분류 (test·typo·dummy·duplicate·paid …). NULL = 분류 안 함. 파기가 아니라 열람 표시다. ⚠ 기본 목록에서 빠지는 건 앞의 넷뿐 — paid(기결제자)는 표시만 하고 남는다. 어느 쪽인지는 애플리케이션(라우트 HIDDEN)이 정한다';
